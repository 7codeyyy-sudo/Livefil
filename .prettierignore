# Prettier 忽略清单
#
# 原则：格式化只覆盖「本项目正在开发与维护的代码和配置」。
# 正式文档与既有原型不参与自动格式化，避免产生无业务价值的整文件差异
# （见 FND-007：提交前检查同样不得自动格式化 doc/ 下的正式文档）。

# ── 正式文档与既有原型 ──────────────────────────────────────────────
# 人工维护的调研、需求与设计文档：排版由作者决定，格式化会重排表格与换行。
doc/
# 已冻结的静态原型（doc 之外的历史产物），非本期任务范围，保持逐字节原样。
prototype/

# ── 团队协作过程稿 ──────────────────────────────────────────────────
# 岗位报告、台账与流转文档：人工维护，格式化会重排表格与换行。
team/

# ── 依赖与构建产物 ──────────────────────────────────────────────────
node_modules/
.next/
out/
dist/
build/
coverage/
# 测试产物兜底：项目内配置已把它们指向 .cache/，此处防止绕过项目脚本直接调用工具时
# 在项目根生成目录，并让格式化避开其中的 trace 与截图等二进制内容。
test-results/
playwright-report/
blob-report/

# ── 项目内运行数据与缓存 ────────────────────────────────────────────
.runtime/
.cache/
.data/

# ── 工具与包管理器生成的文件 ────────────────────────────────────────
# 由 npm 维护，格式化会与 npm 自己的序列化结果互相打架。
package-lock.json
# 由 Next.js 生成。
next-env.d.ts
# husky 生成的钩子引导层（程序产物，非人工维护）。
.husky/_/

# ── 压缩产物 ────────────────────────────────────────────────────────
*.min.js
*.min.css
