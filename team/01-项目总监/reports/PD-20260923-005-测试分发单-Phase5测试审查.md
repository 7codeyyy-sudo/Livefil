---
编号: PD-20260923-005
日期: 2026-09-24
来源: 项目总监
去向: 测试交付岗
类型: 任务分发单
状态: 执行中
关联: 分发单 PD-20260923-004（Phase 5 聚合开发，其交付 RD-20260923-003 已终审通过）；RD-20260923-002（5 项裁定已决策闭环）；任务 SYNC-000、SYNC-001~005、UI-008；《UI 页面规范》v0.20 §4.9；《接口文档》§12/§12.1（v0.3）；《数据库设计文档》§4.18（v0.3，含 §4.18.3 勘误）；《详细设计说明书》§5/§5.4；开发任务清单 §8 与 §1.3 DoD
---

## 背景

Phase 5（SYNC-001~005）聚合开发已完成：交付 **RD-20260923-003** 经总监预审通过（附 3 条件/移交）、最终决策者终审四项确认（2026-09-24），代码已随 PR #43 合入 main（CI 必需检查门禁已过）。按 PD-20260923-004 交付方式「聚合开发、**一次测试审查**、单 PR 多 commit」，现下发测试审查分发单。

本期铁律不变：**`tests/` 只有测试交付岗可以修改**；开发侧两处缺陷修复与 12 条建议测试点已随 RD-20260923-003 提出，用例由本岗落地。

## 先阅读

- team/00-公共部分.md、team/00-流转规范.md
- **RD-20260923-003 交付报告全文**（`team/03-全栈研发/reports/`）——重点 §七 12 条建议测试点、§八 检查结果、§九 风险与未完成事项、§六 真机验收与两处缺陷根因、预审意见与终审结论
- PD-20260923-004 分发单的**终审冻结口径 10 条**（及其变更记录中的 5 项裁定放行）
- RD-20260923-002（风险上报与 5 项裁定：ESLint 载体、墓碑偏差声明、tasks keyset 索引、ConfirmDialog 加性扩展、conflict 走 release）
- doc/04-product-design/UI页面规范.md v0.20 **§4.9**（4.9.1 六态横幅/4.9.2 冲突浮层/4.9.3 分工，文案冻结）
- doc/05-technical-design/接口文档.md **§12/§12.1**（含 §12.1.4 勘误：conflict 走 release、manual_merge 返 400 语义）
- doc/05-technical-design/数据库设计文档.md **§4.18**（含 §4.18.3 勘误：墓碑分类更正、tasks 索引表述更正、9 个 keyset 索引登记）
- doc/05-technical-design/详细设计说明书.md **§5/§5.4**（IndexedDB 三 store、离线边界实测、§5.4.3 勘误）
- doc/06-development-plan/开发任务清单.md §8 Phase 5、§1.3 DoD 11 项
- doc/03-requirements/需求规格说明书_SRS.md（FR-001、NFR-REL-004、§10.2、§12）

## 测试范围

1. **SYNC-001~005 全部任务**：三端点（pull/push/resolve）、墓碑传递、LocalStore 三 store、六态横幅与冲突浮层、幂等与冲突、跨设备四条验收。
2. **SYNC-000 spike 结论验证**：S1 零新依赖（→测试点 18）、S2 安全滞后窗口（→测试点 2）、S3 `sync:` 前缀与 `pending_operations` 命名消歧（→测试点 4、18）、S4 离线边界口径（→测试点 9 的负向断言）。
3. **UI-008 规范符合性验证**（→测试点 15）：§4.9 冻结文案与形态逐条符合。

## 测试点清单（总监定稿 18 条，2026-09-24 终审确认）

> P0 必测＝测试报告必须有其通过/失败结论；标记「回归」者须有**集成或 e2e 实跑证据**，不接受仅单元层通过。

| # | 测试点 | 等级 |
|---:|---|---|
| 1 | 【架构断言】页面不得直接访问 `indexedDB`：`app/**`、`src/modules/*/presentation/**`、`src/shared/ui/**` 违规标识符即失败（唯一例外 `local-store.indexeddb.ts`）；以删除式探针断言 `eslint.config.mjs` 的 `INDEXED_DB_ACCESS_RULES` 生效（临时写违规 → lint 变红 → 删除恢复全绿） | P0 |
| 2 | 安全滞后窗口防漏读：构造「事务 A 先取 `changeAt`、事务 B 更晚取时刻并先提交」，断言 A 提交后仍能被 pull 拉到（`changeAt <= now() - lag` 生效，S2 唯一正确性前提） | P0 |
| 3 | push 逐条幂等与单条失败不回滚整批：同批一条 `already_applied` + 一条 `applied` + 一条 `rejected`，HTTP 整体 200、各条状态独立、已 applied 不回滚 | P0 |
| 4 | `sync:` 保留前缀：普通写请求带 `Idempotency-Key: sync:xxx` 被拒；push 前缀行为符合契约 | P0 |
| 5 | `conflict` 条目幂等落态：走 `release` 不 `complete`、不写快照 → 同 `operationId` 重试重评估；冲突未解决复用同一 `conflictId`，解决后转 `applied` | P0 |
| 6 | 同 `operationId` 异指纹 → 该条 `rejected`（`VALIDATION_ERROR`） | P0 |
| 7 | **缺陷 1 回归护栏**：`hasPostgresErrorCode` 单元测试（单层/多层包装 23505/23503、不匹配码、cause 成环、超深度、非对象入参）＋**集成实跑**：同 `Idempotency-Key` 重放普通写端点返 409 `IDEMPOTENCY_REPLAY`、同名生活领域返 409 `CONFLICT` | **P0 回归** |
| 8 | 冲突二选：`keep_server` 只标解决不写实体；`keep_local` 无条件覆盖（`baseVersion: null` 不产生新冲突）；`manual_merge` 返 400；重复解决返 409；非本人冲突返 404 | P0 |
| 9 | 离线续用 e2e：已打开页面断网 → DOM 未变、离线横幅原文出现、已预取路由软导航、恢复联网横幅消失；**不得写成「断网刷新后仍可用」**（无 SW，口径 2 排除——负向断言同步验证 S4 结论） | P0 |
| 10 | **缺陷 2 回归护栏**：断网 POST 白名单集合 → 入队 `create`、快照 `pending`、页面立现；联网 push → 服务端以**客户端 UUID** 落行 → 快照转 `synced` → **刷新/回切后列表该项仅出现一次（去重断言）**；`POST /tasks/batch` 等深层路径 POST **不**入队 | **P0 回归** |
| 11 | LocalStore 事务规则：一次读改写同一事务；`markSnapshotSynced` 对不存在/已 synced 静默忽略；`applyRemoteChange` 对 `pending` 快照一律 `ignored`（本地未确认改动不被远端覆盖） | P0 |
| 12 | 墓碑传递四类：软删 5 表、`schedule_blocks` 状态即墓碑、`execution_logs` 只读无墓碑、`life_areas`/`goals` `deleted` 恒 `false`（§4.18.3 口径）；级联硬删按父级墓碑清理子行 | P0 |
| 13 | **SYNC-005 局域网形态四条复核**（专项 A）：手机或第二台设备经局域网访问开发服务器，复核四条（PC 建草稿另一端可见／一端离线创建恢复后另一端可见／同操作重发不重复写入／冲突有明确提示不静默覆盖）；②③曾在同机双浏览器形态修复后通过，须在本形态重验 | **P0** |
| 14 | **db:test 陈旧断言修复**（专项 B）：`tests/db/phase2.db.test.mjs` 断言空库仅 2 表 → 按现行迁移链产物（13 表）修正断言；修复后 `npm run db:test` **全绿** | P0 |
| 15 | **UI-008 §4.9 规范符合性**：六态横幅文案逐字对 §4.9.1 冻结文本；冲突浮层三按钮（稍后处理／保留服务器版本／保留此设备版本）+ 说明行 + 初始焦点「稍后处理」+ `role=alertdialog`；状态互斥与优先级（离线独占）；Toast 分工（无「已恢复联网」Toast）；**禁措辞扫描**（无「N 项修改待同步」「已存本机待同步」类改写）；零新增令牌扫描 | P0 |
| 16 | **ConfirmDialog 默认行为回归**（RD-20260923-002 裁定 4 承诺项）：默认两按钮/文案/焦点行为逐字节不变（既有语义）、新增 5 个可选 props 各自生效、可选项缺省时零差异 | P0 |
| 17 | pull 分页契约：`limit ≤ 100` 钳制、游标 `"<changeAt毫秒>:<uuid>"` 格式校验、跨页无重复无遗漏、空页与末页边界、无效游标错误语义 | P0 |
| 18 | spike 结论静态验证：零新依赖（锁文件零改动核验）、全仓 `pending_operations` 命名消歧扫描（无混淆命名）、全仓零 `serviceWorker` 引用 | P1 |

## 专项要求（终审确认，逐条执行）

1. **A — SYNC-005 局域网勾选**：测试点 13 复核**通过后**，执行 `doc/06` §8 SYNC-005 四条验收框的勾选。**终审特授权**：本条勾选为验收动作，测试交付岗**仅就该四行**执行（附本分发单编号与 QA-20260924-001 引用），不得改动 doc/06 其他内容（doc/06 其余归全栈研发）；若复核**未通过**，四框保持未勾选，缺陷按流转规范上报总监。
2. **B — db:test 陈旧断言**：由本岗修复（测试点 14），属测试资产维护，不算改他岗代码。
3. **D — 两缺陷回归护栏**：测试点 7（缺陷 1）、测试点 10（缺陷 2）为 **P0 回归必测**，必须附集成/e2e 实跑输出证据。
4. **DoD 门禁**：本报告通过预审与终审前，**Phase 5 不宣告最终 DONE**（DoD 第 4 条以本测试审查补平）。
5. **bug 处置**：发现业务代码缺陷**不得自行修改 `src/`/`app/`**——按流转规范写入测试报告 bug 列表，经总监转全栈研发另起修复 PR（main 已含 PR #43，不回写历史）。

## 允许修改的目录

- `tests/`（全部，含修复 `phase2.db.test.mjs`、新增 18 条测试点用例）
- `team/04-测试交付/`（本岗报告）
- `doc/06-development-plan/开发任务清单.md`：**仅限** §8 SYNC-005 四条验收框的勾选（专项 A 授权）

**禁止**：`src/`、`app/`、`drizzle/`、`scripts/`、composition-root、`doc/` 其余部分、其他岗位 `team/` 目录、git 配置与 `.git/`、`package.json`（若测试确需新依赖，停止并上报总监，不得自行加依赖）。

## 验收标准（测试报告须完整包含）

1. **测试项清单**：18 条测试点逐条列出执行状态（通过/失败/阻塞/不适用及原因）。
2. **通过/失败数**：汇总计数（含单元、集成、e2e 分层）。
3. **bug 列表与严重等级**：每条含复现步骤、期望/实际、所属模块；严重等级四级——**致命**（数据丢失/核心链路不可用）、**严重**（功能缺陷无 workaround）、**一般**（功能缺陷有 workaround）、**轻微**（文案/体验）。
4. **`npm run check` 与 `npm run test:browser` 实际输出计数**：粘贴真实运行的通过/失败/跳过数字与退出码，不接受引用他人数据。
5. **`npm run db:test` 全绿输出**（测试点 14 修复后）。
6. **上线建议三档**（择一并给理由）：① 可直接上线；② 修复列出的 N 个问题后上线；③ 暂不建议上线。
7. **SYNC-005 勾选执行记录**（局域网复核结论 + doc/06 四框状态）。
8. 报告写到 `team/04-测试交付/reports/QA-20260924-001-Phase5测试审查报告.md`，**自包含**，状态：**待预审**，关联本分发单与 RD-20260923-003。

## 执行方式

1. 先通读「先阅读」全部材料与 RD-20260923-003 全文，再列**测试计划与用例文件清单**动手。
2. 修复 db:test 陈旧断言后，先跑 `npm run db:test` 确认全绿，再落 18 条测试点用例。
3. 局域网复核需要运行中的开发服务器与可用 PostgreSQL（Docker `livefil-pg` 形态见 RD-20260923-003 §六 环境说明），无法达成环境时**停止并上报**，不得以同机双浏览器形态替代（那是研发已做的形态）。
4. 遵循既有测试资产风格；不动 `fnd-003` 锁定的统一命令形态（事故记录 003）。
5. 交付后提交 QA-20260924-001，由总监登记台账并预审 → 终审 → 闭环 PD-004/PD-005。

## 请求事项

1. 按本分发单测试范围与 18 条定稿清单执行 Phase 5 测试审查；对清单有异议或发现契约冲突，**停止并上报**总监，不得擅自增删测试点或收窄形态要求。
2. 交付 QA-20260924-001（状态：待预审），由总监登记台账并预审 → 终审。

## 变更记录

- 2026-09-24 项目总监按 RD-20260923-003 终审结论（四项确认）创建本分发单，状态：执行中，去向：测试交付岗。测试点清单 18 条为总监定稿（研发建议 12 条 + 总监补充 6 条），专项 A/B/D 与 DoD 门禁均经终审确认。
- 2026-09-24 测试岗交付先行件《Phase 5 测试计划与用例文件清单》（`team/04-测试交付/reports/QA-20260924-001-Phase5测试计划与用例文件清单.md`）。总监计划评阅（协调意见，不走预审/终审流转；正式验收对象仍为最终测试报告）：**18/18 测试点全覆盖、执行顺序正确（先修 db:test）、停报条款与分发单一致，计划总体合格**。**须纠正 1 项**——测试点 13 行「②③ 同机重验 + ①④ 局域网形态」不满足分发单「以局域网形态复核四条」：**②③（离线创建恢复后另一端可见、同操作重发不重复写入）必须同样在局域网/真第二端形态重验**，不得以同机形态替代，同机结果仅作对照不作验收依据。**注意 3 项**（不阻塞）：① 层归属标注三处不一致（测试点 1 文件在 `tests/unit/architecture/` 却标 Integration、16 文件在 `tests/integration/` 却标 Unit、15 文件在 `tests/e2e/` 却标 Integration）——纯标注问题，终报告计数分层须与实际文件路径一致；② 交付物「16 个新增用例文件」实为 **15 个**（去重后，push-operations 与 sync-offline 各承载多行），终报告核对数字；③ 建议测试点 2（安全滞后窗口）除单元外**增一条真实 PostgreSQL 的 integration 用例**（注入时钟构造后提交场景），确保索引+查询端到端成立——列为建议非强制。计划文档与最终报告同用编号 QA-20260924-001：可接受，但终报告须自包含并作为该编号主文档。
