# npm 配置。本文件不含任何本机绝对路径。
#
# cache 使用相对路径，由 npm 在项目根目录解析为 .cache/npm。
# 通过 scripts/env/npm.mjs（npm run env:npm -- ...）执行时，还会额外注入
# npm_config_cache 绝对路径，二者指向同一目录，实际生效值由 env:check 校验。

cache=.cache/npm

# 关闭捐助提示，减少与开发无关的输出。
fund=false
