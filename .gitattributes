# 行尾规范化
#
# 为什么需要这个文件：本机 Git 默认 core.autocrlf=true，会把检出到工作区的文本文件
# 转成 CRLF，而仓库内保存的是 LF。结果是同一个文件在 Windows 与 Linux 之间来回转换，
# 产生大量与业务无关的整文件差异，git diff 与 code review 都会被噪声淹没。
#
# `text=auto eol=lf` 的含义：
#   text=auto —— 由 Git 自动判定哪些文件是文本；是文本就在入库前做行尾规范化。
#   eol=lf    —— 决定检出方向：工作区也写 LF。该属性优先级高于 core.autocrlf，
#                因此不再依赖每个成员的本地 Git 配置。
* text=auto eol=lf

# 二进制文件：禁止行尾转换，否则会破坏文件内容
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.webp binary
*.woff  binary
*.woff2 binary
*.ttf  binary
*.pdf  binary
*.zip  binary
*.sqlite binary
*.db   binary

# 由 Next.js 自动生成，不参与人工 review
next-env.d.ts linguist-generated=true
