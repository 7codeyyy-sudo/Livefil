# Livefil 开发任务清单

> 版本：v0.5  
> 日期：2026-09-17  
> 用途：作为 AI 辅助开发的唯一任务入口之一  
> 变更：v0.2 展开 UI-001 交付物与验收（对应 T-002 样式方案确认、《UI 页面规范》§2.4）；v0.3 随《UI 页面规范》§2.4 修订，令牌清单更新为 18 个颜色令牌 + 2 个阴影令牌，明确断点验收边界与两批提交分工，UI-002 增加主按钮配色待决项；v0.4 关闭该待决项——主按钮定稿近黑实心，通过组件别名令牌取色；v0.5 UI-002 展开为四批分工（对齐《UI 页面规范》v0.5 §2.4 令牌收编批次），明确令牌基建、纪律扫描扩展与 styleguide 窄屏验证决策
> 关联：[SRS](../03-requirements/需求规格说明书_SRS.md) · [概要设计](../05-technical-design/概要设计说明书.md) · [详细设计](../05-technical-design/详细设计说明书.md)

## 1. 使用规则

### 1.1 AI 每次只处理一个任务

给 AI 开发任务时，应提供：

1. 本文件中的任务 ID。
2. 任务目标和依赖任务。
3. 关联的 SRS/设计文档。
4. 允许修改的目录。
5. 验收标准。
6. 必须执行的测试和检查。

不允许 AI 一次跨越多个阶段，也不允许在没有确认的情况下顺便扩大需求范围。

### 1.2 推荐开发提示词

```text
请执行任务 <TASK-ID>。

先阅读：
- doc/README.md
- doc/03-requirements/需求规格说明书_SRS.md
- doc/05-technical-design/概要设计说明书.md
- doc/05-technical-design/详细设计说明书.md
- 本任务指定的关联文档

要求：
1. 先检查当前代码和已有改动，不覆盖用户已有工作。
2. 先给出实现计划和将修改的文件。
3. 只实现本任务，不顺便实现其他任务。
4. 遵循 universal-code-quality 规范。
5. 为核心逻辑补充测试。
6. 执行格式化、静态检查、测试和构建。
7. 最后报告修改内容、测试结果、风险和未完成事项。
```

### 1.3 完成定义（Definition of Done）

每个任务只有在以下条件全部满足时才能勾选：

- [ ] 功能符合任务描述和验收标准。
- [ ] 相关输入经过边界校验。
- [ ] 失败路径有明确错误和用户反馈。
- [ ] 核心逻辑有单元测试或集成测试。
- [ ] 没有把业务逻辑写进页面组件。
- [ ] 没有硬编码密钥、路径、金额或时区偏移。
- [ ] 涉及钱使用整数分/Decimal，涉及时间使用 UTC 和用户时区。
- [ ] 涉及外部网络调用时有超时、限流和降级。
- [ ] 迁移、删除和数据清理有备份/回滚方案。
- [ ] 格式化、lint、test、build 均通过。
- [ ] 文档、接口或数据库变更已同步。

## 2. 阶段总览

```text
Phase 0 基础工程
      ↓
Phase 1 设计系统与应用外壳
      ↓
Phase 2 身份、设置与生活领域
      ↓
Phase 3 任务、目标与收件箱
      ↓
Phase 4 排程、例程与执行记录
      ↓
Phase 5 云端数据、离线草稿与同步
      ↓
Phase 6 开销记录与目标关联
      ↓
Phase 7 日/周复盘
      ↓
Phase 8 提醒与通知
      ↓
Phase 9 AI 智能向导与建议
      ↓
Phase 10 安全、测试、部署与邀请测试
```

P0 阶段完成后才进入 P1。AI 功能不能阻塞核心手动闭环。

## 3. Phase 0：基础工程

目标：建立可持续开发、测试、迁移和部署的基础。

### ENV-001 开发环境路径检查（P0）

依赖：无

- [x] 确认项目根目录由启动脚本解析，不使用写死的本机绝对路径。
- [x] 确认项目依赖安装在项目下的 `node_modules`。
- [x] 确认包管理器缓存和构建缓存有项目目录配置。
- [x] 确认本地数据库数据、日志、备份和导出目录位于项目下。
- [x] 确认 `.env.local` 不被 Git 跟踪。

关联：[开发环境规范](./开发环境规范.md)

### ENV-002 固定运行时和包管理器（P0）

依赖：ENV-001

- [x] 固定 Node.js 版本。
- [x] 固定 npm/pnpm 其中一种包管理器。
- [x] 提交对应 lock 文件。
- [x] 优先使用项目目录下的便携运行时或明确记录系统运行时路径。

### ENV-003 配置项目本地目录（P0）

依赖：ENV-002

- [x] 创建 `.runtime`、`.cache`、`.data` 目录。
- [x] 配置本地日志和导出路径。
- [x] 配置数据库开发数据路径或托管数据库连接。
- [x] 配置 `.gitignore` 防止秘密和运行数据提交。

### FND-001 初始化 Next.js 项目（P0）

依赖：无

- [x] 创建 TypeScript + Next.js App Router 项目。
- [x] 配置路径别名和严格类型检查。
- [x] 配置环境变量读取和校验。
- [x] 创建本地开发启动命令。

验收：`dev`、`build`、`start` 可以正常运行，非法环境变量会明确报错。

### FND-002 建立代码质量工具链（P0）

依赖：FND-001

- [x] 配置 ESLint。
- [x] 配置 Prettier 或等价格式化工具。
- [x] 配置 EditorConfig。
- [x] 配置 TypeScript strict 模式。
- [x] 配置统一的 lint、format、typecheck、test、build 命令。

验收：新代码可以被统一格式化，lint 和 typecheck 能在本地执行。

### FND-003 建立测试框架（P0）

依赖：FND-001

- [x] 配置单元测试框架。
- [x] 配置组件/集成测试基础。
- [x] 配置 E2E 测试基础。
- [x] 建立测试数据工厂和随机测试用户。

已确认方案（2026-09-15）：

| 用途 | 选型 |
|---|---|
| 单元/集成测试 | Vitest 5.0.1，需显式安装其必需 peer `Vite 8.3.0` |
| React 组件测试 | `@vitejs/plugin-react` 6.1.1、React Testing Library 16.3.3、user-event 14.6.7、jest-dom 7.0.1、jsdom 30.0.1 |
| HTTP 拦截 | MSW 2.15.0，仅用于 Node 侧测试 |
| E2E | Playwright 1.63.0 |
| 覆盖率 | `@vitest/coverage-v8` 5.0.1 |

约定：

- 测试与测试工厂落在 `tests/` 下（`unit`、`integration`、`e2e`、`factories`、`helpers`、`setup`），不放进 `src/`。
- `vitest.config.ts` 分 unit（node 环境）与 integration（jsdom 环境）两个 project；`playwright.config.ts` 只匹配 `tests/e2e`，单 worker，先 build 再 start。
- 脚本名 `test:unit`、`test:e2e` 不得变更；`check` 不跑浏览器，`check:all` 含浏览器。
- E2E 浏览器安装到项目内 `.cache/playwright`，覆盖 Playwright 默认的系统级目录。
- 覆盖率产物输出到 `.cache/coverage/`，P0 不设通过阈值。
- MSW 只用于 Node 侧测试，不替代依赖注入，也不作为 E2E 的拦截手段；E2E 统一使用 `page.route()`。
- 已有 `tests/unit/validation/env.test.ts` 迁移到 Vitest；既有 `.mjs` 验收脚本继续保持 node:test。

验收：至少有一个单元测试、一个 API 测试和一个页面 E2E 测试通过。

### FND-004 建立模块目录和依赖边界（P0）

依赖：FND-001、FND-002

- [x] 创建 `src/modules`、`src/shared`、`src/infrastructure`。
- [x] 创建任务、目标、排程、执行、开销、复盘、同步、AI 模块目录。
- [x] 页面不得直接依赖数据库实现。
- [x] 领域层不得依赖 Next.js 页面组件。

验收：目录结构和依赖方向与概要设计一致。

### FND-005 建立错误和日志规范（P0）

依赖：FND-002

- [x] 定义领域错误和 API 错误码。
- [x] 建立结构化日志接口。
- [x] 增加 request ID。
- [x] 默认脱敏用户内容、密钥和令牌。
- [x] 禁止空 catch 和静默吞错。

验收：测试接口失败时返回统一错误结构，日志不包含敏感内容。

### FND-006 建立 CI 基线（P0）

依赖：FND-002、FND-003

- [x] 配置 lint、typecheck、test、build 的自动执行。
- [x] 构建失败时阻止合并。
- [x] 记录 Node、包管理器和数据库迁移版本。

验收：提交一个故意失败的测试可以验证 CI 会阻止合并。

> 完成证据（2026-09-16）：探针 PR #1 中故意失败的单测使 `ci-gate` 变红，分支保护强制禁用合并（对 owner 同样生效），PR 已关闭、探针分支已删除，未进入主干（提交 `ebb2e34`）。CI 基线与仓库可见性记录见《开发环境规范》§8 和《工程决策记录》001。

### FND-007 接入提交前检查（P1）

依赖：FND-002

- [x] 接入 Husky 或等价 Git hooks 工具。
- [x] 接入 lint-staged 或等价 staged 文件检查工具。
- [x] pre-commit 只检查 staged 代码和配置文件。
- [x] pre-commit 执行格式化和 ESLint 自动修复。
- [x] pre-commit 不执行完整 typecheck、test 和 build。
- [x] 避免自动格式化 `doc/` 下的正式调研、需求和设计文档。

验收：修改一个 staged 源码文件时，提交前会自动格式化并执行 lint；未 staged 文件不会被误改。

## 4. Phase 1：设计系统与应用外壳

目标：先建立 PC Web 的高级简约视觉和移动端可扩展布局。

### UI-001 建立设计令牌（P0）

依赖：FND-001

- [x] 在 `src/shared/ui/styles/tokens.css` 建立 18 个颜色令牌（9 基础语义色 + 9 派生/辅助色）与 2 个阴影令牌，字体/间距/圆角令牌一并建立，命名与浅色取值逐一对齐《UI 页面规范》§2.4，由根布局唯一导入；组件使用 CSS Modules，不引入 Tailwind 或其他样式框架（T-002 已确认）。
- [x] 不得预造 §2.4「暂不收编」中的令牌（状态色边框、半透明白磨砂）。
- [x] 断点不做成 CSS 变量：CSS（含组件 CSS Modules）按 §3.1 px 写媒体查询，不算第二份取值；仅当 JS 需要时新增唯一 TS 常量镜像。
- [x] 第一阶段实现浅色主题，`:root` 声明 `color-scheme: light`。
- [x] 深色只预留 `html[data-theme='dark']` 占位块，不实现切换：仓库内不得设置 `data-theme` 属性、不得出现 `prefers-color-scheme`（验收脚本断言）。
- [x] 令牌纪律：`tokens.css` 之外禁止 hex/rgb/hsl 字面量（含内联 style），由验收脚本断言。
- [x] 扩展 `scripts/env/check.mjs` 的硬编码绝对路径扫描到 `app/`、`src/`（`.ts`/`.tsx`/`.css`，暂不含 `tests/`）。
- [x] 所有状态颜色同时有文字或图标表达（人工评审项，无法自动判定）。

交付批次（沙箱开发者只交文件、不做 git 写，由仓库维护者在真机分两次提交）：

- 批次 A `chore(env):` 仅含 check.mjs 扫描面扩展，作为独立提交先落地；验收为 `env:check` 对 `app/`、`src/` 零 FAIL 且 `test:e2e` 全绿（四个 fnd 验收脚本内含 env:check 断言，CI 无独立 env:check 步骤）。
- 批次 B `feat(UI-001):` 含 tokens.css、根布局唯一导入、UI-001 验收脚本，在批次 A 合并后开工。

验收：

- `tokens.css` 存在并被根布局唯一导入；§2.4 的 18 个颜色令牌与 2 个阴影令牌全部有定义且浅色取值逐行一致，字体/间距/圆角覆盖 §2.2–§2.3；
- 机器断言通过：令牌文件外无颜色字面量、无深色激活点（`data-theme` 设置点与 `prefers-color-scheme`）、断点像素值在 `.ts/.tsx` 中仅出现在唯一 TS 镜像文件（CSS 媒体查询不受限）；
- `env:check` 扩展后对 `app/`、`src/` 零 FAIL（误报先人工确认，再定行内豁免约定，不提前建豁免机制）；
- `npm run check` 全绿。

关联：[UI 页面规范](../04-product-design/UI页面规范.md) · T-002（[产品与技术设计决策清单](../05-technical-design/产品与技术设计决策清单.md)）

### UI-002 建立基础组件（P0）

依赖：UI-001

按四批交付（批次 3 因浮层体量拆 3a/3b 两个 PR，共五个 PR；与《UI 页面规范》v0.8 §2.4「UI-002 令牌收编批次」对齐）：

- [x] 批次 1：Button、IconButton、Input、Select、Textarea；并完成令牌基建——主按钮别名 3 枚、动效 3 枚（`--duration-fast/--duration-base/--ease-standard`）、尺寸 3 枚（`--size-control-sm/md/--size-touch-min`，≤767px 在 tokens.css 单点覆盖 sm→44px）落 tokens.css；扩展 `tests/e2e/ui-001-acceptance.mjs`：令牌族白名单加 `--duration-/--ease-/--size-/--z-`，新增「tokens.css 之外禁止 ms 时长字面量与数字 z-index」扫描（须配删除式探针自证）；建 styleguide 夹具骨架（见下）。
- [x] 批次 2：Badge、Progress、Tabs 三个组件（**Pagination 移出本批，挂账 UI-005**：规范 §4 与原型均无分页形态，SRS NFR-PERF-006 允许「分页或分段加载」，待收件箱真实列表场景再在数字翻页器与「加载更多」之间定夺；若选翻页器，先更新规范 §4 再实现）。同 PR：①收编状态色边框 3 枚（`--color-success/warning/danger-border`，语义色 24% alpha），颜色令牌契约由 18 变为 21，`tokens.test.ts` 期望值同 PR 更新；②`--radius-pill: 999px` 落 tokens.css（规范 v0.7 §2.4 圆角令牌表，Badge 与 Progress 消费）；③Badge 文字用 `--font-size-caption`（原型 11px 归整 13px，不新增第六级字阶）；④Tabs 按真 Tab 行为实现（`role=tablist/tab/tabpanel`、左右方向键切换），视觉沿用原型分段控件（surface-soft 容器 + 激活项 shadow-raised，原型 `.add-tabs/.add-tab`）；⑤扩展 `tests/e2e/ui-001-acceptance.mjs`：新增「tokens.css 之外禁止 px 数字字号（`font-size: <n>px`）与 px 数字圆角（`border-radius: <n>px`），圆角放行 `50%`/`0`/`inherit`/`var()`」扫描，须配删除式探针自证；styleguide 同页追加三组件分区。
- [x] 批次 3a：Modal、ConfirmDialog 两个组件（形态以《UI 页面规范》v0.8 §4.5 为准）；同 PR ①落 `--z-scrim`、`--z-overlay`、`--duration-slow` 到 tokens.css（**`--z-toast` 不提前落**，随 3b 消费者）；②建浮层内部基建（Portal 挂载、背景滚动锁、焦点陷阱、ESC 关闭、关闭后焦点归还、进退场延迟卸载）供 3b 复用，不对外导出通用浮层框架；手写 ARIA，不用原生 `<dialog>`；ConfirmDialog 为 Modal 特化（`role=alertdialog`、`aria-describedby`、危险操作初始焦点在「取消」）；Modal **不画原型磨砂白边**（v0.8 §2.4 冻结，挂账 UI-003，禁止用 `--color-border` 顶替）；③vitest 行为测试覆盖开关/ESC/遮罩关闭/Tab 焦点循环与焦点归还/滚动锁/退场动画结束再卸载，styleguide 追加分区，双 project 用 computed style 验证层级、遮罩色、`--radius-lg` 与 reduced-motion 下进退场归零；④新令牌受既有纪律扫描保护（扫描器批次 1 已建，无需新扫描），须配删除式探针自证。
- [x] 批次 3b：Drawer、Toast 两个组件（实现以《UI 页面规范》v0.8/v0.9 §4.5 为准）；`--z-toast` 随 Toast 落 tokens.css；Drawer 桌面右侧滑入 `min(440px,100vw)`、≤767px 全屏、位移用 `--duration-base`，**header/body/footer 三段纵列、贴边圆角 0**，复用 3a 遮罩/滚动锁/焦点基建，**不做方向引擎**（250px 左导航抽屉连同磨砂侧栏归 UI-003）；Toast 公共 API 为 **`<ToastProvider>` + `useToast()`**（根布局客户端 island；show/success/error 返回 id、dismiss，不做命令式单例），底部居中、新条在最底部、最多 3 条（满则淘汰最老自动关闭条，**错误常驻条豁免淘汰**；悬停/聚焦暂停计时），`role=status`/错误 `role=alert`、不夺焦，关闭时长为组件内命名常量（普通 5s/带操作 8s/错误常驻），变体以前导语义图标区分（内联 SVG currentColor：成功 `--color-success`/错误 `--color-danger`，背景一律 surface，不做 warning 变体），只提供操作槽不内置撤销逻辑（操作文字 `--color-accent`）；两组件进退场均为退场延迟卸载（动画结束再卸载，退场期间仍引用 `--duration-*`，不得写字面量）；测试与 styleguide 要求同 3a，含队列上限与淘汰豁免、暂停计时、计时器清理、关闭后无残留节点，provider 用例验证 useToast 必须在 provider 内（失配显式抛错）。
- [x] 批次 4：EmptyState、LoadingState、ErrorState 三个页面状态组件，同模块导出 Skeleton 骨架原语（形态以《UI 页面规范》v0.11 §4.6 为准，**零新增令牌**）；EmptyState 复刻原型虚线框（body/medium 标题 + text-secondary 描述，不加图标，`action`/`secondaryAction` 槽均可选）；LoadingState 为**静态骨架、零循环动画**（容器 `role=status` + `aria-busy` + 视觉隐藏加载说明，Skeleton 用 surface-soft 底 + 既有圆角由调用方组合，不做 shape 预设；进行态仍归 Button loading/Progress/Toast）；ErrorState 无衬底、`role=alert`、danger 图标为唯一红色、重试槽用近黑 primary；操作一律 ReactNode 槽，不收 `{label,onClick}` 配置对象；vitest 覆盖三态结构/ARIA/槽位与 Skeleton 组合，styleguide 追加分区，双 project 用 computed style 验证虚线框、骨架底色、无循环动画（reduced-motion 同验）。
- [x] 全部组件支持键盘焦点和移动端触控尺寸。

开工约束（主按钮配色已于 2026-09-17 定稿，见《UI 页面规范》v0.5 §2.1/§2.4）：主按钮为近黑实心白字（`#1D1D1F` / hover `#000000`），只允许通过组件别名令牌 `--color-primary-surface`、`--color-primary-hover`、`--color-on-primary` 取色，不得直接把 `--color-text-primary` 当表面色；强调色蓝只用于状态/进度/焦点，不用于普通操作按钮。动效、尺寸、层级同样只能引用 §2.4 令牌，禁止「先在组件写值、后补令牌」。

窄屏与五态验证（v0.5 决策）：

- 新增 `app/styleguide/page.tsx` 组件展示夹具（Playwright 需要构建后路由，env 门控在 CI 全新构建下不可行）：不进任何产品导航，`metadata.robots` 设 `noindex`，每批在同一页追加分区；它是测试夹具不是展示项目，同样受令牌纪律扫描约束。
- 新增 `tests/e2e/styleguide.spec.ts`：desktop-chromium 与 mobile-chromium 双 project 验证五态（默认/禁用/加载/错误/窄屏），断言 computed style 与几何值（含移动端主控件高度 ≥ 44px、`reducedMotion: 'reduce'` 下时长归零）；禁止截图断言（跨平台渲染差异）。
- 行为层（ARIA、键盘、label、`role="alert"`、五态切换逻辑）由 vitest + Testing Library 负责，与浏览器层不互替。
- **styleguide 去留结论（UI-002 收尾复审，v0.11 定稿）：保留为常驻视觉回归夹具**。noindex、不入产品导航、无生产入口三条守卫维持现状（不入导航有浏览器断言守卫）；退出标准为**逐组件迁移**——某组件的视觉断言随真实页面建设迁入后，才删除该组件在 styleguide 的分区；全部组件迁完才删除整个 `/styleguide` 路由与夹具文件。在此之前它是组件唯一的浏览器视觉守卫，不得整体提前删除。

验收：组件有默认、禁用、加载、错误和窄屏状态，五态在 styleguide 双视口浏览器用例中可见；每批本地 `npm run check` 与 `test:browser` 全绿。

**UI-002 收尾（2026-09-18）：完成。**

- 交付：五个代码 PR（#13 批次1 / #15 批次2 / #17 3a / #19 3b / #22 批次4），共 15 类组件 + Skeleton 形状原语——Button、IconButton、Input、Select、Textarea、Badge、Progress、Tabs、Modal、ConfirmDialog、Drawer、Toast、EmptyState、LoadingState、ErrorState，以及 LoadingState 同模块的 Skeleton（index.ts 共 16 个对外值导出）。
- 终态测试计数（合并点 c28a142，合并后本地基线复跑）：unit 235、integration 141、node:test e2e 71/71；浏览器 desktop 55+1skip、mobile 56（共 111+1skip）。
- 令牌/扫描器终态：颜色/字号/圆角/时长/层级五族字面量扫描在位（ui-001 验收用例覆盖），令牌经 §2.4 各批次收编、零逃逸、零未规划新增。
- styleguide 三守卫核查（v0.11 结论执行）：`noindex`（[page.tsx](../../app/styleguide/page.tsx) metadata `robots`）、不入导航（浏览器断言在基线绿）、无生产入口均维持；保留为常驻视觉回归夹具，退出标准同上文 v0.11 结论（逐组件迁移、全部迁完才删路由）。
- 遗留（不影响 UI-002 完成判定）：UI-003 承接 Modal 磨砂白边与原型 250px 左导航抽屉；UI-005 承接 Pagination 形态；开发者沙箱两条环境观察（`.next` 旧产物致删除计数提前耗尽、验收脚本按工具调用一脚本一条命令）属沙箱跑法，非产品缺陷。

### UI-003 建立应用外壳（P0）

依赖：UI-001、UI-002

- [x] PC 左侧导航：≥1024px 展开 232px 侧栏（不透明 surface、border 右边框），导航项当前页 `aria-current="page"` + surface-soft 高亮（形态以规范 v0.13 §3.2 为准）。
- [x] 顶部工具栏和快速添加入口：桌面/平板右对齐当前页操作与「＋ 快速添加」主按钮；本批只交入口，行为随 TASK-002 接入。
- [x] 窄屏折叠导航/底部导航预留：768–1023px 收为 72px 首字母栏（文字 `font-size:0`、`::first-letter`、无图标库）；320–767px 流式侧栏 `display:none` 不渲染（v0.13 勘误，非 v0.12 文本的移出视口），汉堡（aria-expanded/controls）唤出 250px 左抽屉，遮罩/ESC/焦点陷阱/焦点归还/延迟卸载复用浮层基建；底部五项导航不预造。
- [x] 页面最大宽度和响应式布局：页面区 `min(--layout-max-width,100%)` 居中（1200）；三档网格切换，reduced-motion 下抽屉位移归零；磨砂不画（两笔挂账销账），零新增令牌。
- [x] 路由切换和当前页面状态：`(app)` 路由组承载外壳，`/` 重定向 /today，六目录（today/inbox/goals/expenses/review/settings）交付「建设中」占位页；导航高亮随 usePathname，不伪造页面内容。

验收：双 project 浏览器在**真实路由**上验证三档（不再走 styleguide）——1280px 展开列与 232px 宽、820px 折叠列 72px 与首字可见/文字视觉隐藏、390px 抽屉唤出（250px、遮罩、ESC 关闭、焦点进出与归还）；本地 `npm run check` 全绿（基线 unit 235 / integration 141 / node:test 71）。

**UI-003 收尾（2026-09-20）：完成。**

- 交付：规范 PR #24（v0.12 冻结）+ 代码 PR #25（一个 PR 交付，merge `70016a5`）：`(app)` 路由组外壳（AppShell 三档网格 + Sidebar/Topbar/MobileNavDrawer）、六个「建设中」占位页、`/`→`/today` 307 重定向、globals.css 全局重置；浮层基建新增 `edge-left` 落点供左抽屉复用。
- 审查发现并修复 1 个阻断缺陷：抽屉内点导航/品牌链接只切路由、抽屉不关闭（滚动锁残留）；经 `onNavigate→onClose` 把四条关闭路径（导航链接/品牌/ESC/遮罩）汇拢修复，integration +2 例、双 project e2e +2 例回归。
- 终态测试计数（合并点 70016a5，合并后本地基线）：unit 235、integration 155（基线 153+2 修复回归，清单验收行所写 141 为写清单时旧基线）、node:test e2e 71/71、build 完整；浏览器 desktop 64+1skip、mobile 65（app-shell 11 例双 project 全绿）。
- 批准偏离三处（已全部记档）：① 手机档流式侧栏用 `display:none` 而非冻结文本的 `translateX` 移出（规范 v0.13 已勘误同步，理由见 §3.2：屏外链接可 Tab + 双份导航/双 aria-current）；② 顶栏与页面区共用 1200px 容器；③ 根布局加 `title.template`。
- 已知边界（接受）：抽屉打开后拉宽视口越过 768px，汉堡消失但抽屉残留（ESC/遮罩可关，不困住用户），项目有断言禁 TS/TSX 断点像素值，不为此写镜像断点。
- UI-005 预决策：页面级操作（pageActions）走 **React context + portal 的 PageActions 岛**（外壳顶栏槽放 ref+context，页面渲染 `<PageActions>` 投射内容），不走并行路由；Pagination 形态仍随收件箱真实列表定夺。

### UI-004 建立空、加载和错误状态（P0）

依赖：UI-002、UI-003

- [x] 取数原语 `useAsyncQuery`（形态以规范 v0.14 §4.7 为准）：客户端 hook，fetch **真实版本化 URL**，AbortSignal 随卸载/queryKey 变化/refetch 中止（abort 不入错误态）；返回 loading/error/success 判别联合与 `refetch()`；不缓存、不后台刷新、不自动重试、不引 SWR/React Query。落点 `src/shared/ui/components/AsyncState/`（不新增 src/shared 子目录）。
- [x] 四态容器 `<AsyncState>` 接入五个真实页面（today/inbox/goals/expenses/review，替换 UI-003 占位页；**settings 不动**）：只消费状态、不发请求，按四态组合 §4.6 组件；空态文案/操作、错误描述、骨架轮廓由页面提供，不内置泛化文案。各页查询定义（URL+DTO）暂放 `app/(app)/_lib/`，Phase 3 迁入 `src/modules/*/presentation`（记债）。
- [x] 今日无任务：EmptyState，具体下一步只接**真实存在**的目标（如链接 `/inbox`）；不伪造快速添加按钮（随 TASK-002）。
- [x] 收件箱为空：EmptyState；**inbox 本批只做状态、不做列表**——成功非空分支经 renderSuccess 占位（本批返回 null，集成测试覆盖该分支），列表与输入框由 UI-005 交付。
- [x] 无目标、无开销、无复盘：三页 EmptyState，文案按各页语义给具体下一步。
- [x] API 加载中：LoadingState（`role=status`/`aria-busy`）+ 各页用 Skeleton 组合的页面轮廓；零循环动画。
- [x] API 失败和重试：ErrorState（`role=alert`，danger 仅图标）+ **primary** 重试按钮接 `refetch`；无拦截时未匹配 URL 的 FND-005 结构化 404 即真实失败源，不建返回空数据的假端点。
- [x] 无网络提示：`useOnlineStatus`（`navigator.onLine` + online/offline 事件，SSR 安全）+ `<OfflineBanner>`——持久内联非模态横幅，挂内容区顶部、`role=status`、离立即现/上线自动消失。**「待同步提示」拆出挂 Phase 5（sync 模块），不计本批验收。**

验收：集成测试（MSW，Node 侧）覆盖四态、重试 refetch、abort 不落错误态、离线横幅出现与消失；双 project 浏览器 e2e 在**真实路由**上用 Playwright `page.route()`（不启用浏览器端 MSW）：200 空数组→EmptyState 文案与操作、延迟 fulfill→骨架可见与 aria-busy、500 ErrorState→点重试→第二次 fulfill 成功；离线经浏览器上下文离线模拟验横幅。`npm run check` 全绿（**终态** unit 235 / integration **173** / node:test 71；行内 155 为批次前旧基线）。

UI-004 收尾（2026-09-20）：

- 规范 PR #27（v0.14 冻结 §4.7）、代码 PR #28（merge `6f1b500`）、文档 PR（v0.15 勘误 + 接口文档 + 本记录）。
- 审查裁决三处（均确认开发正确）：① 端点按接口文档 `GET /tasks?from&to`/`?status=inbox`，规范原举例 `/tasks/today` 系笔误，v0.15 勘误；② 日复盘「成功但无内容」为 200 `data:null`（404 是失败），接口文档已补明；③ 容器补 `errorTitle`/`onRetry` 必填，v0.15 同步。
- 五页接线经删除式临时探针真机核验（无拦截五页均 ErrorState、拦截空结果均本页 EmptyState），探针已删；page-state 双 project 各 5 例。
- 记债：查询定义 Phase 3 迁入 `src/modules/*/presentation` 并以服务端 schema 推导 DTO；日期参数格式（ISO vs 时间戳）随 SYNC-001 对齐；`navigator.onLine` 不探真实连通性（已知边界）；10s 超时路径未真机直测（逻辑核验）。
- 「待同步提示」拆给 Phase 5（sync 模块），用户确认；五页非空成功分支本批不可达。
- `.cache/stale/` 6 项（约 280MB）已清。

## 5. Phase 2：身份、设置与生活领域

目标：建立用户数据边界、时区、语言和基础分类。

> **交付方式调整（2026-09-20，用户决定）：** 鉴于逐任务推进进度缓慢，Phase 2 不再按 IAM-001~004 分四批，改为**一次性开发 → 一次测试审查 → 单个 PR**；为保留可审查性，提交必须按模块拆成多个 commit（单 PR 多 commit），建议顺序：① DB-001 数据层基建 → ② IAM-001 本地会话与用户初始化 → ③ IAM-002 设置（端点 + /settings 页）→ ④ IAM-003 生活领域（端点 + 设置页分区）→ ⑤ IAM-004 数据边界测试。
> **范围收缩：云端模式整体挂账**（决策 T-006 部分确认）：本批＝**本地单用户批次**，不实现邀请制云端账号、密码/魔法链接认证、邮件通道与真实跨用户隔离。DB/迁移选型＝Drizzle + Drizzle Kit（T-004 已确认）。基线 main `4169a5b`。

### DB-001 数据连接与迁移基建（P0，2026-09-20 新增）

依赖：FND-004

- [ ] 接入 Drizzle ORM 与 Drizzle Kit：连接配置走环境变量（经现有 env 校验，含连接超时），服务端连接单例；连接失败经 `DependencyUnavailableError` 归一，不裸抛驱动错误。
- [ ] Drizzle schema 与迁移目录落后端位置——**不新增 src/shared 子目录**（FND-004 七子目录契约不可破），具体路径由详细设计指定。
- [ ] 首个版本化迁移建 users、life_areas 两表，字段以《数据库设计》§4.1/4.2 为准；users 补本批所需列：`default_task_duration_minutes`、`default_buffer_minutes`、`ai_enabled`（默认 false）、`ai_data_consent`（默认 false），数据库设计文档同步。
- [ ] 脚本 db:generate / db:migrate；迁移只向前，不改老迁移（详细设计 §11 向后兼容纪律）。
- [ ] **仓储接口与 Drizzle 实现分离**：route handler 只依赖仓储接口，集成测试注入 fake 仓储（默认 check 链不依赖 PG 服务）；Drizzle 实现与迁移的真机验证走单独 db 测试脚本（需 `TEST_DATABASE_URL`），由审查在验收时真机执行。

### IAM-001 本地模式（P0）——云端挂账

依赖：DB-001、FND-005

- [ ] 首次使用（无有效会话）自动初始化**唯一本地用户**（mode=local）与默认生活领域（见 IAM-003），无需注册；初始化幂等，重复启动不产生第二用户。
- [ ] 本地会话经**签名 HttpOnly Cookie** 维持（签名方案由详细设计定；SameSite/Secure 按环境）；本批不建 sessions 表、不实现 refresh。
- [ ] 明确当前数据模式：`GET /me` 与设置页显示「本地模式」标识，不存在模式歧义。
- [ ] 无 Cookie 时核心功能可用：本地会话解析失败＝**自动建立本地会话而非 401**；401 保留为云端语义（挂账）。
- [ ] 云端模式（邀请账号、多用户授权、`/auth/cloud/*`）**不实现**：请求落入 FND-005 结构化 404，契约位置保留。

### IAM-002 用户设置（P0）

依赖：IAM-001

- [ ] `GET /me` 返回设置与 mode；`PATCH /me` 经 Zod 校验更新：locale、timezone、currencyCode、weekStartsOn、默认任务时长与缓冲、AI 开关与数据发送同意、提醒开关与安静时段。
- [ ] 非法值（不支持的 locale/时区、货币非 3 字母、weekStartsOn 越界、时长非正）返回结构化校验错误；单事务、无部分更新。
- [ ] AI 约束：未取得数据发送同意（ai_data_consent=false）时 ai_enabled 不可开启；安静时段 start/end 合法性校验。
- [ ] /settings 占位页替换为真实设置页（形态以 UI 规范 v0.16 §5「设置页」为准）：分组表单、保存有明确反馈、脏表单离开有提示；时区默认值的首启确认方式记债（users.timezone 标注「由用户确认」）。
- [ ] 设置变更写审计事件（不记敏感内容；落库形态随本批）。

### IAM-003 生活领域（P0）

依赖：IAM-001、IAM-002

- [ ] 默认领域初始化：本地用户首启写入默认领域集合（名称/语义色 key/排序由详细设计给），幂等。
- [ ] 创建、编辑、归档、排序端点（GET/POST/PATCH/DELETE `/life-areas`）：Zod 校验；同用户未归档名称唯一→冲突错误；color_key 只存语义 key，不存前端色值。
- [ ] 归档＝置 is_archived（不删行）：历史任务/目标关联保持可解析；归档领域不出现在默认选择器，可经「显示已归档」查看。
- [ ] 排序：sort_order 批量重排为单事务，不产生重复序号。
- [ ] 用户级隔离：查询强制 user_id 作用域（见 IAM-004）；访问非本人领域走 404 语义（不泄露存在性），代码路径本批保留、云端生效。
- [ ] 管理 UI 落在 **/settings 页内分区**（UI 规范 v0.16 §5「设置页」）：列表、新增、重命名、归档、排序；归档有确认（非破坏性、可恢复）。

### IAM-004 数据边界与权限测试（P0）——云端部分挂账

依赖：IAM-001、IAM-003

- [ ] 仓储层：全部查询带 user_id 谓词有测试，并加架构断言（禁止无 user_id 作用域的全表查询，参照现有 dependency-boundaries 测试形态）。
- [ ] 会话边界：无 Cookie / 坏签名 Cookie 的行为符合 IAM-001（自动本地会话或显式错误，分场景断言；不允许静默拿到任意用户上下文）。
- [ ] 写操作：PATCH/DELETE 不存在或已归档资源的错误码；version 乐观冲突返回冲突错误。
- [ ] 敏感设置：AI 数据发送同意、数据导出（如本批实现）的权限与确认路径有测试。
- [ ] **挂账（云端批次补测）**：真实跨用户隔离（A 不访问 B）、云端 401、邀请码流程——本地单用户环境无法真机验证；预留测试结构，不写永远通过的假用例。
- [ ] DB 真机测试（`TEST_DATABASE_URL`）：迁移在空库可执行、仓储 CRUD 与事务回滚、初始化幂等；由审查验收时真机执行，不进默认无 DB 的 check 链。

验收（本批一次完成）：integration 用 fake 仓储（无 PG 依赖）覆盖会话初始化幂等、/me 全字段校验矩阵、领域 CRUD/唯一冲突/归档/排序、错误码与统一信封；node:test 补 phase2-acceptance.mjs（目录契约、无 src/shared 新子目录、迁移文件入库、无云端端点实现）；双 project 浏览器 e2e 在真实 /settings 上验设置表单（保存反馈、脏表单、AI 同意约束）与领域管理（新增/重命名/归档/排序/刷新持久化），经 page.route 打在真实端点契约上；真机 db 测试由审查执行。`npm run check` 全绿（基线 unit 235 / integration **173** / node:test 71，终态计数验收后更新）。

## 6. Phase 3：任务、目标与收件箱

目标：完成从快速输入到目标行动的基础数据闭环。

### TASK-001 任务领域模型和迁移（P0）

依赖：FND-004、IAM-003

- [ ] 创建 tasks 表和迁移。
- [ ] 定义状态枚举和状态流转。
- [ ] 定义任务仓储接口和 PostgreSQL 实现。
- [ ] 增加用户、状态、日期和删除索引。

### TASK-002 收件箱快速添加（P0）

依赖：TASK-001、UI-003

- [ ] 快速创建只要求标题。
- [ ] 可选生活领域、日期、时长和目标。
- [ ] 创建成功后明确反馈。
- [ ] 空标题、过长标题和重复提交有处理。

### TASK-003 任务详情和状态流转（P0）

依赖：TASK-002

- [ ] 查看和编辑任务。
- [ ] 完成、部分完成、延期、跳过、归档。
- [ ] 支持最低版本。
- [ ] 状态修改记录审计事件。

### GOAL-001 目标和行动（P0）

依赖：IAM-003

- [ ] 创建、编辑、暂停、完成和放弃目标。
- [ ] 目标关联生活领域。
- [ ] 创建和管理目标行动。
- [ ] 结果进度与行动进度分开。

### GOAL-002 目标与任务关联（P0）

依赖：TASK-003、GOAL-001

- [ ] 任务可关联目标和行动。
- [ ] 目标详情展示关联任务。
- [ ] 取消关联不删除任务。

### UI-005 收件箱页面（P0）

依赖：TASK-002

- [ ] 列表、空状态、批量安排。
- [ ] 列表加载形态定夺（Pagination 挂账，自 UI-002 批次 2 移入）：对照真实任务列表在数字翻页器与「加载更多」分段加载之间决策（SRS NFR-PERF-006 两者皆可，后端接口文档 §1.3 已备分页响应；分段加载复用 Button 即可，不新增原语）；若选数字翻页器，先更新《UI 页面规范》§4 再实现，Pagination 组件随本任务或其前置小组件 PR 落地。
- [ ] 任务快速转为目标行动。
- [ ] 任务归档。

### UI-006 目标页面（P0）

依赖：GOAL-001、GOAL-002

- [ ] 目标列表。
- [ ] 目标详情。
- [ ] 直接新增行动和今日任务。
- [ ] 展示行动进度而非虚假的结果完成度。

## 7. Phase 4：排程、例程与执行记录

目标：完成核心“计划 → 执行”闭环。

### SCHED-001 时间块模型和查询（P0）

依赖：TASK-003

- [ ] 创建 schedule_blocks 表。
- [ ] 使用 UTC 存储并保留时区语义。
- [ ] 按日/周查询时间块。
- [ ] 设置开始时间和结束时间校验。

### SCHED-002 冲突检测（P0）

依赖：SCHED-001

- [ ] 检测同一用户时间块重叠。
- [ ] 检测固定事项冲突。
- [ ] 返回可解释的冲突信息。
- [ ] 不自动覆盖其他时间块。

### SCHED-003 时间线交互（P0）

依赖：SCHED-001、UI-003

- [ ] 今日时间线。
- [ ] 周视图基础能力。
- [ ] 桌面拖拽移动。
- [ ] 点击编辑作为移动端替代。
- [ ] 显示当前时间线和未安排区。

### ROUTINE-001 例程和重复任务（P0）

依赖：TASK-003、SCHED-001

- [ ] 例程和步骤。
- [ ] 每日/每周重复规则。
- [ ] 修改当前实例或未来规则。
- [ ] 规则变更不篡改历史实例。

### EXEC-001 执行记录（P0）

依赖：TASK-003、SCHED-001

- [ ] 完成、最低版本完成、部分完成、延期、跳过。
- [ ] 实际时长和原因。
- [ ] 可选精力和心情。
- [ ] 追加式保存历史。
- [ ] 幂等提交防止重复记录。

### EXEC-002 恢复模式（P0）

依赖：EXEC-001

- [ ] 建议缩短、最低版本、延期或暂停。
- [ ] 使用中性文案。
- [ ] 不以连续天数归零作为核心惩罚。

### UI-007 今日页面（P0）

依赖：SCHED-003、EXEC-001

- [ ] 当前行动。
- [ ] 时间线。
- [ ] 今日摘要。
- [ ] 快速完成和调整。
- [ ] 超载提示。

## 8. Phase 5：云端数据、离线草稿与同步

目标：保证移动端未来可以复用同一套数据和 API，草稿不会因换设备丢失。

### SYNC-001 云端仓储和 API（P0）

依赖：IAM-001、TASK-001

- [ ] PostgreSQL 作为云端主数据源。
- [ ] REST `/api/v1` 接口。
- [ ] 用户数据隔离。
- [ ] 创建/编辑/删除返回服务端版本。

### SYNC-002 IndexedDB 缓存（P0）

依赖：SYNC-001

- [ ] 保存最近查询数据。
- [ ] 保存待同步草稿。
- [ ] 显示缓存和待同步状态。
- [ ] 核心页面网络失败时不白屏。

### SYNC-003 Outbox 和幂等提交（P0）

依赖：SYNC-002

- [ ] 客户端为操作生成 UUID。
- [ ] 保存操作类型、实体 ID、基础版本和 payload。
- [ ] 网络恢复后批量提交。
- [ ] 重复提交不会生成重复任务、开销或执行记录。

### SYNC-004 冲突处理（P0）

依赖：SYNC-003

- [ ] 任务/目标版本冲突检测。
- [ ] 时间块冲突提示。
- [ ] 执行记录和开销追加式处理。
- [ ] 删除墓碑同步。
- [ ] 用户可选择保留本地、保留服务端或手动合并。

### SYNC-005 跨设备验收（P0）

依赖：SYNC-004

- [ ] PC 创建草稿，另一浏览器可看到。
- [ ] 一端离线创建，恢复网络后另一端可看到。
- [ ] 同一操作重复发送不会重复写入。
- [ ] 冲突有明确提示，不静默覆盖。

## 9. Phase 6：开销记录与目标关联

目标：完成快速记账，并把开销与生活目标连接。

### EXP-001 金额和分类模型（P0）

依赖：IAM-003、FND-004

- [ ] 创建 expense_categories 和 expenses 表。
- [ ] 金额使用最小货币单位整数或 Decimal。
- [ ] 保存 currencyCode。
- [ ] 分类和用户权限校验。

### EXP-002 快速记账（P0）

依赖：EXP-001、UI-003

- [ ] 金额优先输入。
- [ ] 默认分类和最近使用分类。
- [ ] 日期、支付方式和备注。
- [ ] 编辑、软删除和撤销。

### EXP-003 开销关联（P0）

依赖：EXP-002、GOAL-002

- [ ] 可关联生活领域、目标和行动。
- [ ] 关联均为可选。
- [ ] 目标删除不删除历史开销。

### EXP-004 开销摘要（P0）

依赖：EXP-003

- [ ] 月度/周度总额。
- [ ] 分类汇总。
- [ ] 生活领域汇总。
- [ ] 目标关联开销汇总。
- [ ] 不同币种不直接混合汇总。

## 10. Phase 7：日/周复盘

目标：让数据产生下一步调整，而不是只显示统计。

### REVIEW-001 日复盘（P0）

依赖：EXEC-001、EXP-004

- [ ] 展示当天完成和偏差。
- [ ] 记录阻碍和下一步调整。
- [ ] 可跳过。
- [ ] 可编辑和保存。
- [ ] 补全数据库设计文档中 `reviews`、`review_adjustments` 的字段定义（当前该节只有说明文字，缺字段清单）。
- [ ] 按补全后的字段实现 `reviewFactory`（FND-003 阶段只交付最小骨架）。

### REVIEW-002 周复盘汇总（P0）

依赖：REVIEW-001

- [ ] 计划/实际时长。
- [ ] 完成/部分完成/延期/跳过。
- [ ] 重复延期任务。
- [ ] 目标行动进度。
- [ ] 开销摘要。

### REVIEW-003 复盘调整（P0）

依赖：REVIEW-002

- [ ] 保留、缩短、延期、暂停、删除。
- [ ] 调整动作可追溯。
- [ ] 调整后下一周计划可查看。

## 11. Phase 8：提醒与通知

目标：提供低打扰提醒，不让核心功能依赖通知。

### NOTIFY-001 提醒规则（P0）

依赖：SCHED-003、IAM-002

- [ ] 任务提醒。
- [ ] 例程提醒。
- [ ] 复盘提醒。
- [ ] 安静时段。
- [ ] 全局和单条关闭。

### NOTIFY-002 通知失败降级（P0）

依赖：NOTIFY-001

- [ ] 浏览器不支持通知时保留应用内提示。
- [ ] 发送失败有记录。
- [ ] 重试有上限和退避。
- [ ] 不重复轰炸用户。

## 12. Phase 9：AI 智能向导与建议

目标：用 AI 降低输入和整理成本，但不让 AI 成为核心依赖。

### AI-001 确定性新手引导（P0）

依赖：UI-003、IAM-002

- [ ] 引导创建第一个行动。
- [ ] 引导安排时间。
- [ ] 引导完成一次执行记录。
- [ ] 引导第一次复盘。
- [ ] 可以跳过、重新打开和关闭。

### AI-002 上下文帮助（P1）

依赖：AI-001

- [ ] 表单字段解释。
- [ ] 空状态示例。
- [ ] 当前页面下一步提示。
- [ ] 帮助文案不依赖外部 API。

### AI-003 AI Provider 接口和 Mock（P1）

依赖：FND-004、FND-005

- [ ] 定义 provider interface。
- [ ] 建立 Mock Provider。
- [ ] 建立超时、限流和错误处理。
- [ ] 记录调用用量但不记录敏感 prompt。

### AI-004 AI 任务拆解草稿（P1）

依赖：AI-003、TASK-002

- [ ] 输入文本脱敏。
- [ ] 返回结构化草稿。
- [ ] 用户编辑、确认或取消。
- [ ] 确认后调用普通任务创建用例。

### AI-005 AI 排程建议（P1）

依赖：AI-003、SCHED-003

- [ ] 只读取用户主动选择的任务和可用时间。
- [ ] 输出可解释建议。
- [ ] 用户确认后创建时间块。
- [ ] 失败时回退手动排程。

### AI-006 AI 开销解析和复盘建议（P1）

依赖：AI-003、EXP-002、REVIEW-002

- [ ] 生成开销草稿。
- [ ] 生成复盘建议草稿。
- [ ] 金额必须用户确认。
- [ ] 不提供投资、借贷、医疗或心理诊断建议。

## 13. Phase 10：安全、测试、部署与邀请测试

### OPS-001 数据备份（P0）

依赖：SYNC-001

- [ ] 每日自动备份。
- [ ] 备份结果告警。
- [ ] 迁移前手动备份。
- [ ] 恢复演练记录。

### OPS-002 数据导出和恢复（P0）

依赖：SYNC-001、EXP-003、REVIEW-001

- [ ] JSON 导出。
- [ ] 导入预览。
- [ ] 合并导入。
- [ ] 删除前确认和回收。
- [ ] 导入失败可回滚。

### OPS-003 安全检查（P0）

依赖：IAM-004、SYNC-004

- [ ] 用户越权测试。
- [ ] SQL 注入、XSS 和 CSRF 基础检查。
- [ ] 前端包不包含密钥。
- [ ] 日志脱敏检查。
- [ ] 依赖漏洞扫描。

### OPS-004 性能和容量测试（P0）

依赖：REVIEW-002、EXP-004

- [ ] 20 个并发用户基础测试。
- [ ] 5000 条用户记录查询测试。
- [ ] 今日视图 P95 延迟检查。
- [ ] 复盘查询 P95 延迟检查。
- [ ] AI 超时和限流测试。

### OPS-005 跨浏览器和响应式测试（P0）

依赖：UI-007、REVIEW-003

- [ ] Chrome。
- [ ] Edge。
- [ ] Safari。
- [ ] Firefox。
- [ ] PC 宽屏。
- [ ] 手机窄屏模拟。
- [ ] 键盘和触控操作。

### OPS-006 部署和回滚（P0）

依赖：FND-006、OPS-001

- [ ] 测试环境部署。
- [ ] 生产/邀请测试环境部署。
- [ ] 环境变量和密钥分离。
- [ ] 数据库迁移发布流程。
- [ ] 应用版本回滚流程。
- [ ] 错误率、延迟、同步失败和 AI 调用量观测。

### OPS-007 邀请测试（P0）

依赖：OPS-001 至 OPS-006

- [ ] 邀请 5–20 名测试用户。
- [ ] 提供测试说明和隐私说明。
- [ ] 记录激活、7 日使用和 4 周留存。
- [ ] 收集计划/执行/开销/复盘的真实反馈。
- [ ] 每周进行一次需求排序。

## 14. 阶段验收门

### Gate A：工程基础完成

必须完成：FND-001 至 FND-006、UI-001 至 UI-004。

判断：可以稳定开发、测试和部署一个空壳应用。FND-007 属于提交体验增强项，不阻塞 Gate A。

### Gate B：核心手动闭环完成

必须完成：IAM、TASK、GOAL、SCHED、ROUTINE、EXEC 和今日页 P0。

判断：用户可以创建目标、安排任务、执行、记录和恢复。

### Gate C：生活数据闭环完成

必须完成：SYNC、EXP、REVIEW、NOTIFY 的 P0。

判断：用户可以跨设备看到草稿，记录开销，并通过周复盘调整下一周。

### Gate D：AI 增强完成

必须完成：确定性向导；AI 草稿功能可作为 P1。

判断：关闭 AI 后，核心功能仍完整可用。

### Gate E：邀请测试发布

必须完成：OPS-001 至 OPS-007。

判断：数据可备份恢复，权限和性能达到 SRS 目标，才能邀请外部用户。

## 15. 任务状态规范

推荐状态：

```text
BACKLOG → READY → IN_PROGRESS → REVIEW → TESTING → DONE
                                  └──────→ BLOCKED
```

- `BACKLOG`：已识别但尚未满足开发条件。
- `READY`：依赖已完成，设计和验收标准明确。
- `IN_PROGRESS`：当前由一个开发任务执行者处理。
- `REVIEW`：代码和设计等待检查。
- `TESTING`：等待自动或人工测试。
- `BLOCKED`：有明确阻塞原因，必须记录原因和下一步。
- `DONE`：完成定义全部满足。

## 16. AI 开发安全规则

- AI 不得擅自执行破坏性数据库迁移、批量删除或覆盖数据。
- 任何删除、迁移和数据清理先备份、预览影响范围并请求确认。
- AI 不得为了通过测试删除测试、放宽类型或关闭安全检查。
- AI 不得在前端写入 API 密钥。
- AI 不得使用真实用户财务数据作为测试样本。
- AI 修改核心数据模型时，必须同步更新数据库文档、接口文档和迁移测试。
- AI 生成代码后必须报告未覆盖的风险，而不能只报告“完成”。

## 17. 任务 ID 规范

### 17.1 任务 ID 是什么

任务 ID 是开发任务清单中每一个可执行工作项的稳定唯一编号。它不是：

- 用户 ID。
- 数据库主键。
- API 请求 ID。
- Git commit hash。
- AI 会话 ID。

任务 ID 用于让人和 AI 对同一项工作使用相同名称，跟踪依赖、验收、代码提交和问题反馈。

### 17.2 当前任务 ID 格式

```text
<MODULE>-<NUMBER>
```

示例：

| 前缀 | 含义 |
|---|---|
| `ENV` | 开发环境 |
| `FND` | 基础工程 |
| `UI` | UI 和页面 |
| `IAM` | 身份、账户和设置 |
| `TASK` | 任务和收件箱 |
| `GOAL` | 目标和行动 |
| `SCHED` | 排程和时间块 |
| `ROUTINE` | 例程和重复任务 |
| `EXEC` | 执行记录 |
| `SYNC` | 云端数据和同步 |
| `EXP` | 开销 |
| `REVIEW` | 日周复盘 |
| `NOTIFY` | 提醒和通知 |
| `AI` | AI 向导和建议 |
| `OPS` | 安全、测试、部署和运维 |

例如：

- `ENV-001`：开发环境路径检查。
- `FND-001`：初始化 Next.js 项目。
- `TASK-002`：收件箱快速添加。
- `SYNC-004`：同步冲突处理。

### 17.3 使用规则

- 每个任务只能有一个主任务 ID。
- 已发布或已开始的任务不得随意改 ID。
- 新增任务必须使用新的编号，并更新本清单。
- 任务拆分时保留原任务作为父任务，并创建新的子任务 ID，例如 `TASK-002-A`、`TASK-002-B`。
- Git 分支建议使用：`feat/TASK-002-inbox-quick-add`。
- Commit 建议使用：`feat(TASK-002): add quick task creation`。
- AI 提示词必须明确写出任务 ID。
- 任务完成后保留历史，不重复使用旧编号。
