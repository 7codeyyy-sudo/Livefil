# Livefil UI 页面规范

> 版本：v0.18  
> 设计方向：高级简约、浅色优先、PC Web 先行、移动端预留  
> 日期：2026-09-21  
> 变更：v0.2 新增 §2.4 设计令牌的 CSS 命名与深色预留约定（对应 T-002 样式方案确认、UI-001）；v0.3 颜色令牌补齐为基础 9 + 派生/辅助 9（新增 surface-soft、text-placeholder、focus-ring、overlay 与 warning/danger-soft 权威值），新增 2 个阴影令牌、soft 推导规则、暂不收编项；v0.4 定稿主按钮配色为近黑实心（原待决项关闭），主强调色语义收窄为状态/进度/焦点，新增组件别名令牌约定（§2.4），§4.1 同步；v0.5 为 UI-002 新增动效、控件尺寸、浮层层级三族令牌与字面量纪律，状态色边框三色定值（颜色契约批次 2 起 18→21），明确 UI-002 令牌收编批次，§6 增加动效令牌指针；v0.6 组件别名令牌补 `--color-on-danger`（UI-002 批次 1：danger 变体的面上之色不得借用 `--color-on-accent`）；v0.7 为批次 2 新增丸形圆角令牌 `--radius-pill` 与圆角令牌表（§2.3/§2.4），§2.2 字阶表补令牌列并把 Badge 文字由原型 11px 归整到 caption 13px（不新增第六级字阶），字号/圆角 px 字面量纳入批次 2 纪律扫描；Pagination 移出 UI-002，挂账到 UI-005（见《开发任务清单》）；v0.8 为批次 3 冻结浮层组件形态（§4.5）：批次 3 拆为 3a（Modal/ConfirmDialog）/3b（Drawer/Toast）两 PR，§2.4 令牌落地批次同步拆分（`--z-toast` 随 3b 首个消费者落地）；Modal 不画原型磨砂白边（仍属暂不收编，随 UI-003 外壳整体决策）；Drawer 冻结为桌面右侧详情抽屉、≤767px 全屏，原型 250px 左导航抽屉归 UI-003；Toast 形态、时长口径与 ARIA 首次成文；v0.9 为批次 3b 补三项实现级决策：Toast 公共 API 定为 `<ToastProvider>` + `useToast()`（不做命令式单例）、堆叠新条在最底部、满 3 条淘汰最老自动关闭条且错误常驻条豁免淘汰、悬停/聚焦暂停计时；状态变体只以前导语义图标（currentColor 内联 SVG，普通无图标/成功 success 色/错误 danger 色）区分，背景一律 surface，不做 warning 变体；Drawer 面板冻结为 header/body/footer 三段纵列、贴边面板圆角 0；v0.10 勘误：分隔线令牌名误写为不存在的 `--color-line`，3 处（§2.4 禁用示例、§4.5 Drawer header、《开发任务清单》3a）统一更正为 tokens.css 实际命名 `--color-border`（无形态变更）；v0.11 为批次 4 新增 §4.6 页面状态组件：EmptyState 复刻原型虚线框、LoadingState 静态骨架（零循环动画）+ Skeleton 原语、ErrorState 单点危险色与近黑重试，操作统一 ReactNode 槽、零新增令牌；styleguide 夹具去留有结论（保留为常驻视觉回归夹具，逐组件迁移、迁完才删路由）；v0.12 为 UI-003 重写 §3.2 冻结应用外壳：三档视口——≥1024px 展开 232px 侧栏、768–1023px 折叠为 72px 首字母栏（不引入图标库、不造 SVG）、320–767px 汉堡唤出 250px 左抽屉（浮层同标准）；顶栏与快速添加入口、`(app)` 路由组与六个占位页、页面区 1200px；原型磨砂（侧栏 `rgba(255,255,255,.66)`、Modal 白边）不采纳并销账，零新增令牌（232/72/250 为外壳 CSS 单点布局量）；原型媒体档 900/640 以 §3.1 断点 1024/768 为准；§3.3 标注移动端已落地项；v0.13 为 UI-003 实现勘误：§3.2 手机档原写「流式侧栏 `translateX(-100%)` 移出视口」，与实现不符——实际为流式侧栏 `display:none` 整体不渲染、导航只由浮层抽屉承担。理由：移动端既已走浮层抽屉（关闭即卸载），若仍把流式侧栏平移到屏外，① 屏外链接仍可被 Tab 聚焦（WCAG 焦点顺序缺口）；② 导航在 DOM 中存在两份，`aria-current` 当前项不再唯一。同步《详细设计》§2 路由组名 `(web)`→`(app)`（无形态变更）；v0.14 为 UI-004 新增 §4.7 冻结「取数原语 + 四态容器 + 离线提示」：客户端自建轻量 `useAsyncQuery`（fetch 真实版本化 URL、AbortSignal、手动 refetch，不引 SWR/React Query，不做缓存/自动重试）；`<AsyncState>` 只消费判别状态、按四态组合 §4.6 组件（文案/操作/骨架由调用方提供，不内置泛化内容）；`useOnlineStatus` + `<OfflineBanner>` 为持久内联非模态横幅（内容区顶部、warning 语义、`role=status`，上线自动消失），「待同步 N 条」不做、随 Phase 5 sync 模块。落点 `src/shared/ui/components/AsyncState/`（不新增 src/shared 子目录）；本批不新增业务 API，加载/错误态经 MSW（集成）与 Playwright `page.route`（双 project e2e）验证；零新增令牌；v0.15 为 UI-004 实现勘误（无形态变更）：① §4.7 举例的 `/api/v1/tasks/today` 不是已定义端点（《接口文档》只有 `GET /tasks` + status/from/to…，写 `/tasks/today` 会落进 `GET /tasks/{taskId}` 语义），改为「`GET /api/v1/tasks?from&to`（当日）」；② §4.7 的容器 props 列举补 `errorTitle`（必填）与 `onRetry`（必填，接 refetch）——ErrorState.title 必填与容器不内置文案两约束合起来只能由调用方给；③ 页面接线层（PageQuerySection）的 `renderSuccess` 入参是**整个信封**（含 meta 游标，UI-005 分页读它）而非裸 data，容器自身 renderSuccess 入参仍为 data；④ 重试按钮固定文案「重试」为组件内置标签（非「状态文案」，不违反不内置泛化文案）；⑤ 补 10s 超时指针（api-client，依据《详细设计》§6.2「所有网络调用必须有超时」，AbortSignal.any 合成使超时落错误态、不被误判为 abort）。同步《接口文档》补「当日无复盘」响应语义；v0.16 为 Phase 2 交付方式调整冻结页面形态：Phase 2（IAM-001~004）改为一次性批次开发（单 PR、按模块多 commit），范围收缩为**本地单用户批次**——云端邀请制、认证方式、邮件通道与真实跨用户隔离挂账（决策 T-006 部分确认）；新增 §5「设置页（/settings）」：顶部「本地模式」标识，分区独立保存（生活领域管理、地区与语言、任务默认值、提醒与安静时段、AI 与隐私），生活领域管理落在该页分区（inline 新增、就地重命名、可达排序基线、轻确认归档/恢复），AI 开关受数据发送同意约束，加载/错误经 §4.7；数据导出/账户删除不随本批、不放假按钮。零新增令牌；v0.17 补 Phase 2 冻结稿四处实现缺口：①新增**分类色族 6 令牌**（§2.1：blue/green/amber/violet/teal/rose，`--color-cat-*`）——生活领域的识别色与状态语义色分家，本批只定主色值一档，soft/border 随真实消费者补，color_key 合法集合＝此六 key；②默认生活领域名单冻结为 SRS 术语表六类（工作/健康/财务/关系/兴趣/家务，名称、color_key 分配与播种条件见《详细设计》§4.8）；③ `DATABASE_URL`/`AUTH_SECRET` 分层必填：env 根校验保持 optional，由 DB/auth 基础设施模块在使用时显式强制（见《详细设计》§8.3）；④ users 表补 `version` 列（§数据库设计 §4.1）支撑 /me 乐观并发；v0.18 为 Phase 3 一次性批次冻结页面形态（任务/目标/收件箱，单 PR 多 commit）：①收件箱列表定为「加载更多」分段加载（cursor + Button，Pagination 挂账销项，不做数字翻页器）；②收件箱支持批量安排/批量归档（不批量删除）与单条「转为目标行动」；③目标页结果进度（手动 resultMetric）与行动进度（行动状态汇总）分开展示，不互相覆盖、不自动判定完成。同步《数据库设计》audit_events 勘误（不建表）与状态对照、《接口文档》§4/§5 端点补齐；v0.18 再补开发复核提出的 8 项缺口：①新增 `useCursorListQuery` 累积原语（useAsyncQuery 只能替换）；②补齐状态流转的反向/重开矩阵（含 completed→planned、archived 恢复）；③目标详情定独立路由 `/goals/{goalId}`，§4.5「优先抽屉」矛盾修正；④顶栏「＋」接为跳转 /inbox 聚焦输入框；⑤新增 Checkbox 组件支持多选（零新令牌）；⑥ GET /tasks 固定 createdAt desc、默认 limit 20/上限 100；⑦幂等口径统一为「带 Idempotency-Key 走 idempotency_keys 识别，不带才各自落库」；⑧补 DELETE /tasks 与 DELETE /actions（行动软删自动解链）

## 1. 视觉关键词

克制、清晰、安静、可靠、有呼吸感。

参考苹果产品的设计原则：留白、层次、内容聚焦、低干扰动效和高质量排版；不复制苹果官网的品牌标志、插画、页面素材或具体布局。

## 1.1 减少 AI 味规范

产品应该像一个经过长期打磨的生活工具，而不是 AI 生成的概念展示页。所有 AI 生成的页面和组件都必须通过以下检查。

### 明确避免

- 不使用大面积蓝紫渐变、霓虹发光、玻璃拟态和科技网格作为默认视觉。
- 不使用机器人头像、魔法棒、星星闪光和“AI 魔法”等装饰作为主要视觉语言。
- 不把首页做成聊天窗口或 AI 对话首页。
- 不用大量圆角卡片、标签、徽章和 KPI 数字填满页面。
- 不使用没有真实业务作用的仪表盘、环形图和趋势图。
- 不把所有操作都包装成“智能”“魔法”“自动化”。
- 不使用过于夸张的渐入、漂浮、脉冲和持续循环动效。
- 不使用没有内容依据的插画、人物照片或 AI 生成装饰图。
- 不让 AI 建议使用比用户任务更强的视觉层级。

### 推荐使用

- 中性色为主，一个稳定的品牌强调色为辅。
- 用排版、间距和真实内容建立层次，而不是靠阴影、渐变和装饰。
- 让用户的任务、时间、金额和下一步行动成为视觉主体。
- 使用简洁的线性图标和明确文字，不依赖图标猜含义。
- 让页面保留适度空白，不为了“看起来丰富”添加无关模块。
- AI 建议使用普通的建议区、草稿区或上下文操作，不使用发光按钮。
- 重要操作使用直接动词，例如“拆分任务”“安排时间”“保存建议”。
- 空状态提供具体下一步，不使用泛化的“开启你的 AI 之旅”。

### AI 功能的视觉层级

AI 必须是产品中的辅助能力，不是品牌主角：

1. 用户手动操作始终可用。
2. AI 入口放在任务、排程和复盘的上下文中。
3. AI 结果明确标记为“建议”或“草稿”。
4. AI 结果必须可编辑、取消和撤销。
5. AI 加载状态使用普通的进度反馈，不使用持续闪烁或发光动画。

### 去 AI 味验收清单

- [ ] 去掉无业务作用的渐变、发光和装饰图。
- [ ] 页面首屏能看见真实任务或下一步行动。
- [ ] 页面不是卡片和数字的堆叠。
- [ ] AI 不是首页唯一入口。
- [ ] AI 建议的视觉权重低于用户确认后的正式数据。
- [ ] 所有图标和颜色都有明确语义。
- [ ] 去掉无法解释的动画和“科技感”装饰。
- [ ] 文案像生活工具，而不是营销型 AI 产品。

## 2. 设计令牌

### 2.1 色彩

第一阶段使用浅色主题：

| 语义 | 建议值 | 用途 |
|---|---|---|
| 页面背景 | `#F7F7F5` | 页面整体背景 |
| 主表面 | `#FFFFFF` | 卡片、抽屉、输入区域 |
| 主文字 | `#1D1D1F` | 标题和重要内容 |
| 次文字 | `#6E6E73` | 辅助说明和时间 |
| 边界 | `#E5E5E2` | 分隔线和输入框 |
| 主强调色 | `#1769E0` | 当前状态、进度、焦点、品牌点缀 |
| 成功 | `#2D8A5B` | 完成、同步成功 |
| 警告 | `#B7791F` | 超载、冲突、待处理 |
| 危险 | `#C0392B` | 删除、错误、失败 |

颜色不能成为唯一语义载体；完成、延期和冲突同时使用文字、图标或状态标签。

**主操作配色（v0.4 定稿）**：保存、开始、确认等高强调主按钮使用**近黑实心**——底色 `#1D1D1F`、hover `#000000`、白字，**不使用强调色蓝**。原型的黑按钮已经过整体视觉验收；蓝色在本系统中专指「此刻活着的状态」（当前时间线、进度填充、焦点环），与「动作」分离，避免同屏蓝色按钮与蓝色状态互相抢夺语义，也把全页饱和色面积压到最低（§1「中性色为主，一个强调色为辅」）。

**分类色（v0.17 新增，6 个）**：专用于**生活领域**的识别（领域色点、任务/目标上的领域标记、领域选择器），与上表的状态语义色是两族——success/danger 表达「好/坏」，分类色表达「属于哪一类」，因此即使领域名为「健康」也不使用 danger 红，避免分类标记被误读成错误。

| key | 值 | CSS 令牌 |
|---|---|---|
| `blue` | `#3B6FD9` | `--color-cat-blue` |
| `green` | `#2F8F6C` | `--color-cat-green` |
| `amber` | `#B98328` | `--color-cat-amber` |
| `violet` | `#7A56C2` | `--color-cat-violet` |
| `teal` | `#1F8A8F` | `--color-cat-teal` |
| `rose` | `#C25670` | `--color-cat-rose` |

六色选取为彼此等明度的一组，小面积使用（色点、细标记），不铺大底。**本批只定主色值一档**；分类色的 soft 底/border 变体在出现真实消费者（如按领域着色的图表）时再按「需要才落」补，不预先制造。color_key 的合法集合即上表六个 key；该集合是契约，增减必须经规范升版。

### 2.2 字体

```css
font-family: -apple-system, BlinkMacSystemFont, "Segoe UI",
  "PingFang SC", "Microsoft YaHei", sans-serif;
```

| 层级 | 字号 | 字重 | 行高 | 令牌（取值） |
|---|---:|---:|---:|---|
| 页面标题 | 28–32px | 500 | 1.25 | `--font-size-page-title`（32px） |
| 区域标题 | 18–20px | 500 | 1.35 | `--font-size-section-title`（20px） |
| 正文 | 14–16px | 400 | 1.5 | `--font-size-body`（16px） |
| 辅助文字 | 12–13px | 400 | 1.45 | `--font-size-caption`（13px） |
| 数字摘要 | 24–32px | 500 | 1.1 | `--font-size-figure`（28px） |

第一阶段仅使用 400 和 500 两种字重，避免视觉噪声。

v0.7 决策：原型 `.tag` 文字为 11px，低于五级字阶任一档下限（caption 为 12–13px），11px 中文小字可读性不足，Badge 文字归整到 `--font-size-caption`（13px）；**不新增第六级字阶**，将来确有 sub-caption 真实消费者时按「需要才落」补令牌并更新本表。

### 2.3 间距和圆角

- 基础间距单位：4px。
- 页面内边距：桌面 32px，平板 24px，手机 16px。
- 内容最大宽度：1200px。
- 几何圆角：10px、14px、20px（令牌见 §2.4「圆角令牌」表）；丸形元素（Badge、进度条等端帽完全圆化者）使用专用丸形令牌 `--radius-pill`（999px），不计入三档几何圆角。原型根变量 `--radius: 16px`（用于 `.surface`）为原型期取值，规范冻结卡片/表面圆角为 14px。
- 主要按钮触控高度：至少 40px；移动端主要触控目标建议 44px（控件高度令牌化，取值与移动端覆盖机制见 §2.4）。
- 阴影极少使用，只用于抽屉、浮层和主操作反馈。

### 2.4 设计令牌的 CSS 命名与使用约定

本节把 §2.1–§2.3 的设计令牌落到 CSS 形态（对应 T-002：原生 CSS 自定义属性 + CSS Modules）。令牌唯一定义在 `src/shared/ui/styles/tokens.css`，由应用根布局唯一导入；组件使用 CSS Modules 引用变量，不直接写字面量。

颜色令牌当前共 18 个：9 个基础语义色（对应 §2.1，逐行给出权威取值）+ 9 个派生/辅助色。UI-002 批次 2 收编 3 个状态色边框后扩为 21 个（见下文「状态色边框」）。

基础语义色（9 个）：

| 语义 | CSS 变量 | 浅色取值 |
|---|---|---|
| 页面背景 | `--color-bg-page` | `#F7F7F5` |
| 主表面 | `--color-surface` | `#FFFFFF` |
| 主文字 | `--color-text-primary` | `#1D1D1F` |
| 次文字 | `--color-text-secondary` | `#6E6E73` |
| 边界 | `--color-border` | `#E5E5E2` |
| 主强调色 | `--color-accent` | `#1769E0` |
| 成功 | `--color-success` | `#2D8A5B` |
| 警告 | `--color-warning` | `#B7791F` |
| 危险 | `--color-danger` | `#C0392B` |

派生/辅助色（9 个）：

| 语义 | CSS 变量 | 浅色取值 | 取值来源 |
|---|---|---|---|
| 柔和面（悬停/选中、标签、分段控件、进度轨道） | `--color-surface-soft` | `#F0F0ED` | 原型 `--surface-soft`，6 处使用 |
| 占位文字 | `--color-text-placeholder` | `#A2A2A7` | 原型输入框 placeholder |
| 强调色上的文字/图标 | `--color-on-accent` | `#FFFFFF` | 原型头像文字 `#fff`（主按钮白字见 v0.4 别名 `--color-on-primary`） |
| 强调色柔和底（选中态、浅底容器） | `--color-accent-soft` | `#EAF1FF` | 原型 `--blue-soft` |
| 键盘焦点环 | `--color-focus-ring` | `rgba(23, 105, 224, 0.28)` | 原型 `:focus-visible`（§7 要求焦点可见） |
| 弹层遮罩 | `--color-overlay` | `rgba(29, 29, 31, 0.28)` | 原型 modal-backdrop |
| 成功柔和底 | `--color-success-soft` | `rgba(45, 138, 91, 0.08)` | 原型完成态底色 |
| 警告柔和底 | `--color-warning-soft` | `rgba(183, 121, 31, 0.08)` | 按 soft 推导规则生成 |
| 危险柔和底 | `--color-danger-soft` | `rgba(192, 57, 43, 0.08)` | 按 soft 推导规则生成 |

`*-soft` 推导规则：**状态色柔和底 = 对应语义色在白底上按 8% alpha 叠加的快照**（`rgba(<语义色 RGB>, 0.08)`）。accent-soft 保留原型已验收的定值 `#EAF1FF`，不强行改为纯 8%。焦点环的 3px 宽度与 2px offset 是几何不是颜色，写在 base/组件样式中，不做颜色令牌。

阴影令牌（2 个，只允许这两处阴影）：

| 语义 | CSS 变量 | 取值 |
|---|---|---|
| 弹层阴影（Modal、Drawer） | `--shadow-overlay` | `0 16px 44px rgba(29, 29, 31, 0.10)` |
| 抬升阴影（Tabs 等激活项、主操作反馈） | `--shadow-raised` | `0 1px 4px rgba(29, 29, 31, 0.06)` |

其他令牌族：`--font-*`（字体族、字号、字重、行高，取值见 §2.2 字阶表令牌列）、`--space-*`（4px 倍数）、`--radius-*`（10/14/20px 几何圆角 + 999px 丸形，见下文「圆角令牌」表）。断点不做成 CSS 变量（变量无法用于 `@media` 条件）：CSS（含各组件的 CSS Modules）按 §3.1 的像素值书写媒体查询，这不算第二份取值；仅当 JS 需要读取断点时，维护一份 TS 常量镜像，断点像素值在 `.ts/.tsx` 中只允许出现在该镜像文件。

组件别名令牌（v0.4 引入，UI-002 实现，**不计入 18 个颜色令牌契约**）：

| 语义 | CSS 变量 | 取值 |
|---|---|---|
| 主按钮表面 | `--color-primary-surface` | `#1D1D1F` |
| 主按钮表面（hover） | `--color-primary-hover` | `#000000` |
| 主按钮上的文字/图标 | `--color-on-primary` | `#FFFFFF` |
| 危险实心面上的文字/图标（v0.6） | `--color-on-danger` | `#FFFFFF` |

别名令牌只引用既有色板取值、不引入新颜色；Button 组件只能引用别名，**不得直接把 `--color-text-primary` 当表面色**（文字令牌挪作表面色会造成语义耦合），danger 变体也**不得借用 `--color-on-accent`**（蓝底白字与红底白字是两个语义，哪怕当前取值相同）。将来若配色改判或深色模式翻转，只改别名值，组件零改动。

动效令牌（v0.5 新增，对应 §6）：

| 语义 | CSS 变量 | 取值 | 取值来源 |
|---|---|---|---|
| 快速过渡（hover、按压、聚焦） | `--duration-fast` | `150ms` | 原型按钮/导航 160ms，归整到 §6 区间下界 |
| 常规过渡（抽屉位移等） | `--duration-base` | `200ms` | 原型抽屉 transform 180ms，归整到 §6 中值 |
| 缓慢过渡（浮层进退场） | `--duration-slow` | `250ms` | §6 区间上界，批次 3a 首次消费时落代码 |
| 标准缓动 | `--ease-standard` | `ease` | 原型三处过渡均为裸写 `ease` |

控件尺寸令牌（v0.5 新增，对应 §2.3）：

| 语义 | CSS 变量 | 取值 |
|---|---|---|
| 小控件高度（Button、IconButton） | `--size-control-sm` | `40px` |
| 大控件高度（Input、Select、单行 Textarea） | `--size-control-md` | `48px` |
| 移动端触控下限 | `--size-touch-min` | `44px` |

`--size-touch-min` 是语义保证值而非组件高度：tokens.css 在 `max-width: 767px` 媒体块把 `--size-control-sm` 覆盖为 `var(--size-touch-min)`（与 `--space-page-x` 的媒体覆盖同机制），组件样式自身不写媒体查询改高度。Tabs 等分段控件（原型 34px）不是独立触控目标，不预造尺寸令牌，确有需要时按同一命名规则追加。

浮层层级令牌（v0.5 新增；`--z-scrim/--z-overlay` 批次 3a 首次消费，`--z-toast` 批次 3b 首次消费）：

| 语义 | CSS 变量 | 取值 | 用途 |
|---|---|---|---|
| 遮罩层 | `--z-scrim` | `100` | Modal / Drawer 的 backdrop |
| 浮层面板 | `--z-overlay` | `200` | Modal、Drawer、ConfirmDialog 面板**共用** |
| 全局提示 | `--z-toast` | `300` | Toast（必须浮在一切浮层之上） |

层级只表达"遮罩→面板→全局提示"三档，不为单个组件开数字；原生 `select` 的弹出列表由系统绘制，不分配层级令牌。

圆角令牌（v0.7 补丸形）：

| 语义 | CSS 变量 | 取值 | 用途 |
|---|---|---:|---|
| 小圆角 | `--radius-sm` | 10px | 控件：按钮、输入框、导航项、分段控件内项 |
| 中圆角 | `--radius-md` | 14px | 卡片与表面（原型 `--radius: 16px` 归整到规范冻结值） |
| 大圆角 | `--radius-lg` | 20px | Modal、大面板 |
| 丸形 | `--radius-pill` | 999px | Badge、进度条等端帽完全圆化的元素（原型写 99px，对其 6px/24px 元素够用；令牌取 999px 保证任意高度下都是全圆端帽） |

丸形表达的是"端帽完全圆化"成语，不是第四档几何曲率，不用于普通表面。`50%`（头像、时间线圆点正圆）是几何写法而非半径档位，纪律扫描对其放行；`border-radius` 只允许引用 `--radius-*` 令牌或 `50%`/`0`/`inherit`。

状态色边框（v0.5 定值，批次 2 收编）：

| CSS 变量 | 取值 |
|---|---|
| `--color-success-border` | `rgba(45, 138, 91, 0.24)` |
| `--color-warning-border` | `rgba(183, 121, 31, 0.24)` |
| `--color-danger-border` | `rgba(192, 57, 43, 0.24)` |

推导规则：**状态色边框 = 对应语义色 24% alpha 白底快照**（与 soft 的 8% 规则并列，形成「8% 柔和底 / 24% 边框 / 100% 文字与图标」三级层次）。三色必须同一 PR 一次收编，不单独引入。收编后颜色令牌契约由 18 变为 **21**，令牌单元测试期望值在同一 PR 更新。中性 Tag 仍用次文字 + 柔和面，不新增边框令牌。

UI-002 令牌收编批次（与《开发任务清单》UI-002 四批分工对齐）：

- 批次 1：主按钮别名 3 枚、动效 3 枚（`--duration-fast/--duration-base/--ease-standard`，`--duration-slow` 批次 3a 补）、尺寸 3 枚落 tokens.css；同步把纪律验收脚本的令牌族白名单扩到 `--duration-/--ease-/--size-/--z-`，并新增「tokens.css 之外禁止出现 `ms` 时长字面量与数字 `z-index`」扫描。
- 批次 2：交付 Badge、Progress、Tabs 三个组件（Pagination 移出 UI-002，挂账 UI-005）；`--radius-pill: 999px` 与状态色边框 3 枚同 PR 落 tokens.css，颜色契约由 18 变为 21，单元测试同 PR 更新；Badge 文字使用 `--font-size-caption`（原型 11px 归整 13px，见 §2.2）；纪律脚本新增字号/圆角 px 字面量扫描（须配删除式探针）。
- 批次 3a：`--z-scrim`、`--z-overlay` 与 `--duration-slow` 随 Modal/ConfirmDialog 落 tokens.css；同 PR 建浮层内部基建（Portal 挂载、背景滚动锁、焦点陷阱、ESC 关闭、关闭后焦点归还、进退场延迟卸载），供 3b 复用，不对外导出通用浮层框架。
- 批次 3b：`--z-toast` 随 Toast 落 tokens.css（3a 无 Toast 消费者，不提前落地）；Drawer 与 Toast 复用 3a 基建，进退场采用退场延迟卸载。
- 浮层组件手写 ARIA 与进退场，不使用原生 `<dialog>`：`::backdrop` 无法引用 `--color-overlay`、进退场无法挂接动效令牌，且 Drawer 无对应原生元素，两套行为会造成分裂。
- 「需要才落代码」不等于可以先在组件里写字面量：未收编的值在消费者出现的同一个 PR 收编，禁止「先写值后补令牌」。

暂不收编（明确推后）：

- 半透明白（原型侧栏 `rgba(255,255,255,.66)`、Modal 边框 `rgba(255,255,255,.6)`）：属磨砂效果，与 §1.1 反玻璃拟态条款相关，留到 UI-003 外壳阶段决定。**v0.8 冻结处置**：批次 3a 的 Modal 不画这道白边、以无边框实现（表面白 + `--radius-lg` + `--shadow-overlay` + `--color-overlay` 遮罩已提供分离层次），不得以 `--color-border` 等既有令牌顶替伪造该视觉；是否新增磨砂边框令牌、是否补回，由 UI-003 外壳阶段对磨砂效果整体决策的同一个 PR 决定。原型 250px 左导航侧栏（含其磨砂底）同属 UI-003 外壳，不是批次 3b 的 Drawer。

使用规则：

- 令牌文件之外禁止出现 hex/rgb/hsl 字面量（含内联 style）；原型中的写死值必须先收编为本节令牌才能迁入。
- 令牌文件之外禁止出现 `ms`/`s` 时长字面量与数字 `z-index`：过渡时长、缓动、浮层层级只能引用本节动效/层级令牌，由 UI-002 批次 1 起的纪律验收脚本强制。
- 令牌文件之外禁止出现 px 数字字号（`font-size: 13px` 等）与 px 数字圆角（`border-radius: 10px` 等）：字号只能引用 `--font-size-*`，圆角只能引用 `--radius-*`；圆角放行 `50%`、`0`、`inherit`。由 UI-002 批次 2 起的纪律验收脚本强制（批次 1 组件已全部令牌化，无存量迁移）。
- `prefers-reduced-motion` 只允许在 tokens.css **单点**处理：通过一个媒体查询把三个 `--duration-*` 覆盖为 `0.01ms`；组件样式内不得写减少动效的媒体查询。
- 第一阶段只实现浅色：`:root` 声明 `color-scheme: light`。
- 深色只预留、不激活：tokens.css 保留 `html[data-theme='dark']` 占位块（值留空/注释）；仓库内任何代码不得设置 `data-theme` 属性，也不得使用 `@media (prefers-color-scheme: dark)`——系统深色用户在第一阶段必须看到已验收的浅色。
- 将来激活深色时，主题属性只挂在 `<html>` 单一节点，变量定义与覆盖选择器统一使用 `html[data-theme='dark']`，只替换变量值，引用方零改动。

已决项（v0.4，2026-09-17 产品确认）：主按钮以原型近黑底为准（见 §2.1「主操作配色」），强调色蓝不用于普通操作按钮；Button 通过上文的组件别名令牌取色，不新增基础语义色。

## 3. 页面布局

### 3.1 断点

| 断点 | 用途 |
|---|---|
| 320–767px | 手机竖屏，单列；顶部菜单（§3.2 已落地）/底部导航（预留） |
| 768–1023px | 平板/窄桌面，折叠侧栏 |
| 1024–1439px | 标准桌面，侧栏 + 主内容 |
| 1440px 以上 | 宽桌面，内容最大宽度限制 |

### 3.2 应用外壳（UI-003）

外壳为「左侧导航 + 主内容（顶栏 + 页面区）」。断点以 §3.1 为准（原型取的 900/640 是近似档，实现按 1024/768）；三档视口三种形态。

#### 桌面（≥1024px）：展开侧栏

- 网格列宽 **232px**（原型值，落在原「224–248px」区间内）+ 主内容列 `minmax(0, 1fr)`。
- 侧栏不透明 `--color-surface` 底、右边框 `1px solid var(--color-border)`；纵排结构照原型：品牌、日期、主导航列表，底部设置项与用户区。
- 导航项纵列左对齐：默认 text-secondary；hover 与当前项为 surface-soft 底 + text-primary（当前项 medium）；当前页由 `usePathname` 判定，锚点带 `aria-current="page"`。
- 品牌块（近黑 brand-mark）、头像（accent 圆）保持原型做法，不另造图形。

#### 平板/窄桌面（768–1023px）：折叠侧栏

- 侧栏收窄为 **72px** 单栏（原型值；网格列宽同值），品牌居中。
- 导航项文字以 `font-size: 0` 隐藏，`::first-letter` 16px 显示首字（今日/收件箱/目标/开销/复盘/设置 → 今/收/目/开/复/设）；日期、品牌文字、用户区文字同步隐藏，仅保留图形。
- **不引入图标库、不自造 SVG**：文字仍在 DOM 中，读屏得到完整名称；触控目标 min-height 42px（原型值，≥触控下限）。

#### 手机（320–767px）：抽屉侧栏

- 流式侧栏整体 `display: none`（不渲染、不占网格），导航只由下方浮层抽屉承担；主内容单列，顶栏左侧出现品牌与汉堡按钮。
  - 勘误（v0.13）：v0.12 此处写的是侧栏 `translateX(-100%)` 移出视口。实现改为 `display:none`，理由两条：① 平移到屏外的链接仍可被 Tab 聚焦，焦点顺序里存在看不见的可聚焦元素；② 抽屉内导航与屏外侧栏在 DOM 中同时存在，当前页会有两个 `aria-current`。抽屉本身关闭即卸载（§4.5），不存在第二份常驻导航。
- 汉堡按钮带 `aria-label="切换导航"`、`aria-expanded`、`aria-controls`；唤出为 **250px 左抽屉**（原型值）。
- 遮罩、ESC 关闭、焦点陷阱、关闭后焦点归还、进退场延迟卸载，标准同 §4.5，复用 `_internal/overlay` 基建；左抽屉是独立实现（§4.5 Drawer 组件是右侧详情方向，不套用其方向假设）。reduced-motion 下位移归零（§6）。

#### 顶部工具栏

- 桌面/平板：操作区右对齐，放当前页必要操作与「＋ 快速添加」主按钮。
- 手机：左品牌、右收窄操作区；「快速添加」入口保留，汉堡必在。
- 「快速添加」的行为已于 Phase 3（TASK-002）接入（2026-09-21 决策）：
  点击跳转 `/inbox` 并聚焦快速输入框（hash 指示），不另建全局弹层机制；
  UI-003 阶段的挂空状态结束。

#### 页面区与路由

- 页面区容器宽 `min(var(--layout-max-width), 100%)`、居中；窄于最大宽度时全宽。
- 建 `(app)` 路由组承载外壳；导航六项目录 `/today`、`/inbox`、`/goals`、`/expenses`、`/review`、`/settings` 本批各交付占位页（真实内容分属 UI-005~007 等）；`/` 重定向到 `/today`。
- 占位页明示「建设中」、不伪造内容；路由切换的当前页状态＝导航高亮；页面区不做整页播报，仅加载/错误内容按 §4.6 带各自 ARIA。

#### 磨砂效果：不采纳，挂账销账

- 原型侧栏底 `rgba(255,255,255,.66)` 与 Modal 白边 `rgba(255,255,255,.6)` 均不实现：§1.1 明确避免玻璃拟态；无 `backdrop-filter` 时半透明白在浅色底上没有视觉意义，加 `backdrop-filter` 又属被禁效果。
- 不新增半透明白令牌，§2.4「暂不收编」中该项关闭；UI-002 批次 3a 起的两笔磨砂挂账销账。
- **零新增令牌**：232/72/250 是外壳布局量，集中在外壳 CSS 单点，不做令牌（同媒体查询 px 的处理：布局量非设计取值）。

### 3.3 移动端外壳预留

- 顶部菜单 + 左抽屉已在 §3.2 落地；底部五项导航仍为后续可选项，本批不预造。
- 详情侧栏切换为底部抽屉（§4.5 Drawer 在 ≤767px 全屏形态已覆盖）。
- 时间线横向密度降低，允许按时间段折叠。
- 统计图表优先改成文字摘要和纵向列表。

## 4. 组件规范

### 4.1 按钮

每组操作最多一个高强调主按钮。

- 主按钮：保存、开始、确认。
- 次按钮：编辑、安排、查看详情。
- 低强调按钮：取消、稍后处理、跳过。
- 危险按钮：删除、彻底清除，必须二次确认。

主按钮外观为近黑实心白字（`#1D1D1F` / hover `#000000`，v0.4 定稿，取值与理由见 §2.1、别名令牌见 §2.4）；强调色蓝不用于操作按钮。

按钮文案使用动词，不使用“确定”作为所有场景的默认词。

### 4.2 任务卡片

任务卡片至少展示：

- 任务名称。
- 时间或预计时长。
- 所属领域/目标，可选。
- 当前状态。
- 一个主要操作。

不要在列表卡片中同时展示所有备注、历史、统计和 AI 信息。

### 4.3 时间线块

- 显示开始时间、名称、预计时长。
- 固定事项、任务、例程使用不同图标或标签辅助区分。
- 当前时间线使用一条清晰但克制的强调线。
- 冲突使用图标 + 文案 + 轻量背景，不只使用红色。

### 4.4 表单

- 先展示最少必填字段。
- 高级字段折叠在“更多设置”中。
- 金额输入默认聚焦金额，不要求先选分类。
- 错误信息靠近字段，说明如何修正。
- 保存成功给出非阻塞反馈，避免打断用户下一步。

### 4.5 抽屉和弹窗

- 详情编辑按场景在侧边抽屉与独立详情页之间选用（v0.18 修正：
  原「优先使用侧边抽屉」与 §5「目标详情页」表述矛盾）——
  **目标详情采用独立页面** `/goals/{goalId}`（需要可刷新/分享 URL 与
  完整工作区；列表项的快速查看后续仍可用抽屉承接）。
- 删除、导入、账户删除使用居中确认弹窗。
- AI 结果使用“草稿面板”，展示原文、建议结果、编辑和取消。
- 弹窗不能包含完整复杂流程；复杂流程改用独立页面。

浮层组件形态（v0.8 冻结形态、v0.9 补实现级决策，UI-002 批次 3a/3b 实现；视觉取值只引用 §2.4 令牌，不新写颜色/时长/层级）：

- **Modal（居中模态，批次 3a）**：原型 `.modal-backdrop/.modal` 为据——遮罩 `--color-overlay`、面板宽 `min(480px, 100%)`、`--radius-lg`、表面色底加 `--shadow-overlay`；**不画原型那道 `rgba(255,255,255,.6)` 磨砂白边**（处置见 §2.4 暂不收编）。行为：`role="dialog"` + `aria-modal="true"` + `aria-labelledby`；ESC 与点击遮罩关闭、X 关闭；打开时锁定背景滚动、焦点进入面板并在面板内循环，关闭后焦点归还触发元素；进退场用 `--duration-slow`。
- **ConfirmDialog（确认弹窗，批次 3a）**：Modal 的特化，不另建浮层机制；`role="alertdialog"`，正文经 `aria-describedby` 关联；危险操作（删除、账户删除等）的初始焦点落在「取消」而非确认按钮，防误触；ESC/遮罩关闭语义等同取消。
- **Drawer（详情编辑抽屉，批次 3b）**：**桌面端右侧滑入**，宽 `min(440px, 100vw)`，遮罩、滚动锁、焦点管理与 Modal 同机制；位移用 `--duration-base`（原型抽屉 180ms 归整值），不用 `--duration-slow`；**≤767px 全屏呈现**（移动端在「底部抽屉或全屏页面」中冻结为全屏，避免编辑表单遇虚拟键盘拥挤）。本组件不做左/右/底部方向引擎：原型 250px 左滑物是**移动端导航 sidebar**，连同磨砂侧栏归 UI-003 外壳，届时若复用再补方向参数。**面板结构（v0.9 冻结）**：容器 fixed 右贴边、`height: 100dvh`（回退 100vh），grid 三行 `auto 1fr auto`，表面色底、**圆角 0**（贴视口边缘的锚定面板；0 属纪律扫描放行值，不产生新令牌）；**header**（标题 + 关闭 IconButton，下划 `1px var(--color-border)`，不滚动）、**body**（`overflow-y: auto`，编辑表单唯一落点）、**footer 可选**（上划线 + 右对齐按钮组，钉底使长表单滚动时操作可达）；≤767px 全屏同构；props 与 Modal 镜像（`title/footer/initialFocusSelector/closable`），初始焦点面板本身。
- **Toast（全局非阻塞反馈，批次 3b）**：原型无此形态，按本节冻结实现——底部居中堆叠、同时最多 3 条；表面色底 + `--radius-md` + `--shadow-overlay`，层级 `--z-toast`（须浮于一切浮层之上，挂 body 级容器）；普通/成功态 `role="status"`（等价 `aria-live="polite"`，对应 §7），错误态 `role="alert"`；不夺走页面焦点。自动关闭时长为组件内命名常量、不做 CSS 令牌：普通 5s、带操作按钮 8s、**错误态常驻不自动关闭**（NFR-REL-002：失败必须明确提示）。组件只提供操作槽（如「撤销」），不内置撤销业务逻辑；归档/删除/延期等撤销随真实消费者接线（§6）。进退场与 Drawer 同为退场延迟卸载，进退场时长用 `--duration-fast`（Toast 非模态、不锁背景，反馈应快于模态浮层的 `--duration-slow`）。**公共 API、队列与变体（v0.9 冻结）**：API 为 **`<ToastProvider>` + `useToast()`**——provider 作为客户端 island 在根布局包住 children，`toast.show/success/error(message, options?)` 返回 id，另可 `toast.dismiss(id)`；不做模块级命令式单例（当前不存在非 React 调用方，未来统一请求层需要时再在 provider 下加 emitter 适配）。堆叠：新条在**最底部**（拇指最近）、旧条向上顶；满 3 条时**淘汰最老的自动关闭条，错误常驻条豁免淘汰**——若槽位均被错误条占用，新普通/成功条直接丢弃；**悬停或聚焦某条暂停其计时**，移出后重新计时；每条带手动关闭 ×。状态变体只以前导语义图标区分：普通无图标、成功内联 SVG 对勾取 `--color-success`、错误内联 SVG 警示取 `--color-danger`（图标自包含、`fill: currentColor`，项目不引入图标库）；正文文字一律 `--color-text-primary`，**背景一律 surface**（不铺 soft 底），不做 warning 变体；操作槽文字用 `--color-accent`。每条自身带 role，外层容器不再套 aria-live，避免双重播报。

### 4.6 页面状态（v0.11 冻结，UI-002 批次 4 实现）

EmptyState / LoadingState / ErrorState 是占满内容区的页面/区块级状态，与 Toast（瞬时非阻塞反馈）、Button 的 `loading`（控件级进行态）分工，互不替代。共同解剖：纵列居中，`title` + `description` 两段文字；操作一律以 **ReactNode 槽**传入真实 Button（不接收 `{label,onClick}` 配置对象，以保留 Button 的 variant/loading 等全部能力）；**零新增令牌**，颜色/字号/圆角/间距只引用 §2.4 既有令牌。

- **EmptyState（空状态）**：原型 `.empty-state` 为据——表面底 + `1px dashed var(--color-border)` 虚线框 + `--radius-md`（原型 16px 就近归整 14px）+ 纵 `--space-12`/横 `--space-6` 内边距（原型 56px 就近归整 48px）+ 居中；标题用 `--font-size-body`/medium/`--color-text-primary`，描述用 `--color-text-secondary`；**不加图标**（原型无此形态，无消费者不预造）。§1「空状态提供具体下一步，不使用泛化文案」由描述文案或操作承担：`action`、`secondaryAction` 两个操作槽均可选。
- **LoadingState（加载状态）**：页面/区块首次加载用**静态骨架**，不用 spinner，也不用闪烁、发光、呼吸等循环动画（§6：首次加载不使用无意义的循环动画）。容器 `role="status"` + `aria-busy="true"`，配视觉隐藏的加载说明（等价 `aria-live="polite"`，§7）。同模块导出 **Skeleton** 支撑原语（`--color-surface-soft` 底 + `--radius-sm`，宽高由调用方给定），由调用方用间距令牌组合标题/段落/列表轮廓，**不做 `shape` 预设引擎**。保存、同步、AI 处理的进行态不归本组件（Button loading、Progress、Toast 各有其位）。
- **ErrorState（阻塞式错误状态）**：无衬底边框（虚线框是「这里还没有东西」的空态专属语言）、居中、宽 `min(480px, 100%)` 与 Modal 同族；一枚 `currentColor` 内联警示图标取 `--color-danger`，是组件内**唯一红色**——标题仍用 `--color-text-primary`、描述 `--color-text-secondary`，不做整面红（§1 克制基调，并为删除场景保留 danger 的分量）；容器 `role="alert"`，插入即播报。主操作槽传重试按钮，用近黑 **primary** 变体（重试是肯定动作，danger 只留给破坏性确认），次操作槽可选。

### 4.7 异步取数容器与离线提示（v0.14 冻结，UI-004）

UI-004 **不新增任何业务 API**：本批交付的是「页面如何取数、如何呈现四态、如何提示离线」。状态呈现组件（EmptyState/LoadingState/ErrorState/Skeleton）形态已在 §4.6 冻结，本节只冻结取数与接线契约。三件新东西落同一模块 `src/shared/ui/components/AsyncState/`（**不新增 `src/shared` 子目录**——七子目录清单受 FND-004 验收精确断言）；各页面的查询定义（URL + DTO）暂放 `app/(app)/_lib/`，Phase 3 建立应用层时迁入 `src/modules/*/presentation`（记债）。

#### 取数原语 `useAsyncQuery`

- 客户端 hook：输入 `{ queryKey: readonly string[]; queryFn: (signal: AbortSignal) => Promise<T> }`；返回判别联合 `{ status: 'loading' } | { status: 'error'; error: Error } | { status: 'success'; data: T }` 与 `refetch()`。
- 页面请求**真实版本化 URL**（如当日任务用 `GET /api/v1/tasks?from=<date>&to=<date>`——《接口文档》的 `GET /tasks` 查询参数；**不存在** `/tasks/today` 端点，写它会落进 `GET /tasks/{taskId}` 语义，把 `today` 当任务 ID）；本批不建业务端点，无拦截时页面诚实落入错误态（未匹配路径经 FND-005 兜底返回结构化 404，本身就是真实失败）。不建返回空数据的假端点。
- `AbortController`：卸载、queryKey 变化、手动 refetch 时 abort；**abort 不进入 error 态**（它是替换而非失败）。
- HTTP 非 2xx 与网络错误归一为普通 `Error`（带 message）；**不自动重试、不指数退避**——重试只有用户手动触发。
- **不缓存、不后台刷新、不去重、不乐观更新**；queryKey 本批只用于将来扩展与测试可辨。刻意不引 SWR/React Query（一个四态容器不值得引入依赖与缓存语义决策，真实缓存需求随 Phase 3 数据场景再议）。

#### 四态容器 `<AsyncState>`

- 容器**只消费状态、绝不发请求**（不碰 fetch），保持可在 jsdom 单测：props 为 `state`（上面的判别联合）、`isEmpty(data)`（调用方给空判据）、`renderSuccess(data)`（成功且非空时渲染真实内容），空态配置 `empty`（title/description/action?/secondaryAction?），`errorTitle`（**必填**）与可选 `errorDescription`、`loading` 骨架，以及 `onRetry`（**必填**，接 `refetch`）。`errorTitle` 必填的理由：ErrorState 的标题本身必填，而容器又不许内置文案，两约束合起来只能由调用方给；`onRetry` 不放进 state——state 是可序列化数据，重试是能力。
- 按状态组合 §4.6 组件，**不内置任何泛化文案**（不许出现写死的「出错了」「暂无数据」）：空态的具体下一步、错误描述、贴合页面的骨架轮廓一律由调用方提供——延续 §4.6 的 ReactNode 槽纪律。唯一的内置文本是错误态重试按钮的标签「**重试**」——它是控件标签而非状态文案。
- 重试：错误态主按钮为近黑 **primary**，`onClick` 接 `onRetry`（danger 只留给破坏性确认，同 §4.6）。
- loading：默认 LoadingState；页面应提供用 Skeleton 组合的本页轮廓（§4.6 不做 shape 预设引擎）。
- 成功但为空（空数组等）走 EmptyState；**EmptyState 的操作只能指向真实存在的目标**（如今日空→链接 `/inbox`）。快速添加（TASK-002）、收件箱输入框（UI-005）尚不存在，空操作槽留空，不放假按钮。
- **页面接线层的 `renderSuccess` 入参是整个信封**（页面侧以 `ApiEnvelope<T>` 为数据，故回调收到 `{ data, meta }`——UI-005 分页读 `meta` 游标）；容器自身的 `renderSuccess` 入参仍为 data。
- 取数客户端对单次请求设 **10s 超时**（《详细设计》§6.2「所有网络调用必须有超时」）：经 `AbortSignal.any` 合成「调用方 signal + 超时 signal」，超时不改调用方 signal，故超时正确落错误态、不被误判为 abort。
- 零新增令牌。

#### 在线状态 `useOnlineStatus` 与 `<OfflineBanner>`

- `useOnlineStatus`：`navigator.onLine` 为初值，订阅 `online`/`offline` 事件（`useSyncExternalStore`，SSR/水合安全——服务端一律按在线处理）。不主动探测真实连通性；`navigator.onLine` 各浏览器语义差异（局域网上但实际断网）属已知边界，记档。
- `<OfflineBanner>`：**持久、内联、非模态**——挂在 AppShell 内容区顶部、页面内容之上（不浮层、不遮罩、不阻塞），离立即现、**上（on）线自动消失**。
- 与既有反馈的分工：不是 Toast（瞬时非阻塞）、不是 ErrorState（不阻塞内容区，缓存/已加载内容仍可看）；容器 `role="status"`（离线即播报，等价 polite live region）。
- 文案止于「当前处于离线状态，显示的内容可能不是最新」；视觉用 surface 底 + warning 语义边框，不做整面警告色，零新增令牌。
- **不做**「N 条待同步」、发件箱、全部重试：依赖尚不存在的 sync 模块（Phase 5）。「待同步提示」从 UI-004 拆出挂 Phase 5，用户已确认。

## 5. 页面规范

### 今日页

- 首屏优先显示当前行动和时间线。
- 顶部最多一个主 CTA：“添加”。
- 今日摘要放在时间线下方，不抢当前行动注意力。
- 未安排任务使用轻量列表，不采用大面积警告颜色。

### 收件箱

- 输入框在首屏。
- 任务列表按最近添加排序。
- 每行只保留安排、关联、归档三个高频动作。
- **列表采用「加载更多」分段加载（2026-09-21 决策，Pagination 挂账销项）**：
  首屏取一页，底部一个「加载更多」Button 经 cursor 拉下一段并**追加**；
  不做数字翻页器、不新增原语；全部加载完后按钮隐藏。
  选择依据：收件箱是流式整理场景，精确跳页使用频率低（SRS NFR-PERF-006 两者皆可）。
  取数经**新增原语 `useCursorListQuery`**（与 useAsyncQuery 同目录、同纪律：
  AbortSignal、不缓存、不自动重试；暴露 loadMore/hasMore，queryKey 变化整体重置）——
  useAsyncQuery 的 refetch 是替换语义，无法承担累积。
- 多选经 **Checkbox 组件**（UI-005 前置小件，形态跟随 Input、零新令牌；
  不得用 Switch 承担多选语义）：勾选多条后批量安排与批量归档（批量删除不做）；
  单条可「转为目标行动」，端点与语义见接口文档 §4。

### 目标

- 目标卡片展示结果描述、行动数量和最近进展。
- 不把进度条做成唯一的成功反馈。
- 目标详情页（路由 `/goals/{goalId}`，2026-09-21 定）必须能直接新增今日行动；
  列表项点击进入该页。
- **结果进度与行动进度分开展示（SRS FR-023）**：结果进度取手动维护的
  `resultMetric`（current/target/unit/note，可在详情就地更新），
  行动进度由行动状态汇总；两者上下相邻但标签明确，禁止把行动完成率
  渲染成结果进度，也不据结果数值自动判定目标完成。

### 开销

- 快速记账按钮固定在页面明显位置。
- 记录列表默认按时间倒序。
- 金额使用等宽数字或 tabular figures，便于扫描。
- 分类和目标关联采用可搜索选择，但常用项优先。

### 复盘

- 复盘页面按“看到事实 → 理解偏差 → 做出调整”排序。
- 每个洞察后放一个就地操作。
- AI 摘要必须标注“AI 建议”，并支持编辑和取消。

### 设置页（/settings，v0.16 冻结）

定位：本地模式下的偏好与数据管理中心。页面顶部显示数据模式标识「**本地模式 · 数据仅保存在本环境**」；云端账号区挂账（T-006），本批不出现登录/账号卡片。

页面为纵向分区卡片，每区独立标题、**独立保存**（不做整页一个巨型保存按钮）：

1. **生活领域**（本批核心管理能力）：
   - 领域行：名称（支持就地重命名）、分类色选择（集合＝§2.1 六分类色）、上移/下移排序、归档操作；桌面可增强拖拽，但按钮排序是可达基线。
   - 「新增领域」为区内 inline 输入（名称 + 色选），不跳页；新领域默认 color_key 取 `blue`。
   - 归档经轻确认（非破坏性、可恢复）；「显示已归档」展开归档项并支持恢复。
2. **地区与语言**：locale、timezone（可搜索选择）、currencyCode、weekStartsOn。
3. **任务默认值**：默认时长、缓冲。
4. **提醒与安静时段**：总开关 + 起止时间；开关关闭时时间控件禁用而非移除。
5. **AI 与隐私**：AI 开关（role=switch）；开启时必须先确认「数据发送提示」，未同意时开关保持关闭并附说明文字。

表单纪律：

- 加载/错误经 §4.7 的 AsyncState（数据源 `GET /me`、`GET /life-areas`）；保存中按钮 loading 并防重复提交，保存成功有明确反馈（Toast 或区内确认）。
- 脏状态：有未保存修改的分区给出标记；路由离开脏页须确认。
- 数据导出/账户删除**不在本批**（data-management 模块），不放假按钮；所有控件标签关联、键盘可达。

## 6. 交互和动效

- 常规状态过渡 150–250ms，时长与缓动只允许引用 §2.4 动效令牌，禁止组件内写时长字面量。
- 首次加载不使用无意义的循环动画。
- 保存、同步、AI 处理使用明确的进行中状态。
- 支持 `prefers-reduced-motion`，减少动效：降级在 tokens.css 单点完成（§2.4 使用规则），组件不写 `prefers-reduced-motion` 媒体查询。
- 拖拽操作必须有键盘和点击替代方案。
- 操作成功后提供撤销，尤其是归档、删除和延期。

## 7. 无障碍和兼容性

- 使用语义化 HTML 和原生 button、input、select。
- 键盘可完成创建、编辑、完成、延期和关闭浮层。
- 焦点状态清晰可见。
- 表单字段有 label，错误使用 `role="alert"`。
- 动态保存和同步状态使用 `aria-live="polite"`。
- 不把 hover 作为关键操作唯一入口。
- 支持 Chrome、Edge、Safari、Firefox 目标版本。

## 8. 设计交付规则

每个页面交付必须包含：

1. 默认状态。
2. 空状态。
3. 加载状态。
4. 错误状态。
5. 无权限/未登录状态。
6. 移动窄屏状态。
7. 键盘和触控替代交互。
8. 文案和数据格式说明。
